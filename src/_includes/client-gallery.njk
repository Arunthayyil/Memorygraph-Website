<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<meta name="robots" content="noindex, nofollow"/>
<title>{{ title }} | Client Gallery | Memorygraph Wedding Studio</title>
<link rel="icon" href="/assets/logo/favicon-32.png" type="image/png" sizes="32x32"/>
<link rel="apple-touch-icon" href="/assets/logo/favicon-180.png"/>
<link rel="preconnect" href="https://fonts.googleapis.com"/>
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;1,300;1,400&family=Jost:wght@300;400;500&display=swap" rel="stylesheet"/>
<link rel="stylesheet" href="/assets/css/global.css"/>
<link rel="stylesheet" href="/assets/css/animations.css"/>
<style>
  :root{--paper:#fbf7ef;}
  button,input,textarea,select{font:inherit;}button{cursor:pointer;}

  .screen-lock{position:fixed;inset:0;z-index:500;background:linear-gradient(140deg,rgba(15,56,56,.96),rgba(26,24,20,.96)),url('{{ lock_image or hero_image or "/assets/stories/abhin-gayathry/03.jpg" }}') center/cover;display:flex;align-items:center;justify-content:center;padding:24px;color:var(--ivory);overflow-y:auto;}
  .screen-lock.hidden{display:none;}
  .lock-panel{width:min(560px,100%);padding:56px 48px;background:rgba(244,237,224,.06);border:1px solid rgba(244,237,224,.12);backdrop-filter:blur(24px);text-align:center;}
  .lock-logo{height:48px;width:auto;margin-bottom:36px;filter:brightness(0) invert(1);opacity:.85;}
  .lock-label,.eyebrow{font-size:.62rem;letter-spacing:.32em;text-transform:uppercase;color:var(--gold-2);font-weight:400;}
  .lock-title{font-family:var(--serif);font-size:clamp(2.2rem,7vw,4.2rem);font-weight:300;line-height:1;margin:20px 0;}
  .lock-title em{font-style:italic;color:var(--gold-2);}
  .lock-copy{max-width:380px;margin:0 auto 28px;color:rgba(244,237,224,.6);font-size:.86rem;line-height:1.8;}
  .lock-form{display:flex;flex-direction:column;gap:12px;max-width:360px;margin:0 auto;}
  .lock-form input{width:100%;padding:16px 18px;background:rgba(244,237,224,.08);border:1px solid rgba(244,237,224,.16);color:var(--ivory);text-align:center;font-size:.92rem;letter-spacing:.06em;}
  .lock-form input::placeholder{color:rgba(244,237,224,.35);}
  .lock-form input:focus{outline:none;border-color:var(--gold-2);background:rgba(244,237,224,.1);}
  .lock-form .btn{width:100%;padding:16px;letter-spacing:.28em;}
  .lock-error{display:none;margin-top:10px;color:#ffd1c4;font-size:.78rem;}
  .lock-error.visible{display:block;}
  .lock-hint{margin-top:20px;color:rgba(244,237,224,.4);font-size:.68rem;line-height:1.7;}
  .lock-divider{width:48px;height:1px;background:rgba(244,237,224,.15);margin:24px auto;}

  .btn{border:0;background:var(--teal);color:var(--ivory);padding:14px 24px;font-size:.66rem;letter-spacing:.22em;text-transform:uppercase;transition:all .3s;text-decoration:none;text-align:center;border-radius:3px;}
  .btn:hover{background:var(--teal-deep);}
  .btn.gold{background:var(--gold);color:var(--teal-deep);}
  .btn.gold:hover{background:var(--gold-2);}
  .btn.ghost{background:transparent;border:1px solid var(--line);color:var(--ink);}
  .btn.ghost:hover{border-color:var(--teal);color:var(--teal);}

  .gallery-app{position:relative;z-index:2;min-height:100vh;}

  /* ── Utility Bar (appears on scroll) ── */
  .topbar{position:fixed;top:0;left:0;right:0;z-index:100;display:flex;align-items:center;justify-content:space-between;gap:16px;height:52px;padding:0 32px;background:rgba(244,237,224,.96);backdrop-filter:blur(20px);border-bottom:1px solid var(--line-2);transform:translateY(-100%);transition:transform .4s cubic-bezier(.22,1,.36,1);will-change:transform;}
  .topbar.visible{transform:translateY(0);}
  .topbar img{height:28px;width:auto;display:block;opacity:.6;}
  .top-actions{display:flex;align-items:center;gap:8px;flex-wrap:wrap;justify-content:flex-end;}
  .topbar-home{display:grid;place-items:center;width:34px;height:34px;padding:0!important;}
  .selection-pill{padding:8px 14px;border:1px solid rgba(29,89,89,.15);color:var(--teal);font-size:.58rem;letter-spacing:.18em;text-transform:uppercase;background:rgba(29,89,89,.03);border-radius:3px;}

  /* ── Hamburger Menu (mobile only) ── */
  .topbar-hamburger{display:none;width:34px;height:34px;border:1px solid var(--line);background:transparent;border-radius:4px;color:var(--ink-soft);place-items:center;cursor:pointer;transition:all .25s;flex-shrink:0;}
  .topbar-hamburger:hover{border-color:var(--teal);color:var(--teal);}
  .topbar-menu{display:none;position:absolute;top:100%;right:16px;min-width:200px;background:var(--ivory);border:1px solid var(--line-2);box-shadow:0 12px 40px rgba(26,24,20,.12);border-radius:4px;overflow:hidden;z-index:110;}
  .topbar-menu.open{display:block;}
  .topbar-menu-item{display:flex;align-items:center;gap:10px;width:100%;padding:14px 18px;border:0;background:transparent;color:var(--ink-soft);font-size:.62rem;letter-spacing:.16em;text-transform:uppercase;cursor:pointer;transition:background .2s,color .2s;text-decoration:none;text-align:left;}
  .topbar-menu-item:hover{background:var(--ivory-2);color:var(--teal);}
  .topbar-menu-item svg{flex-shrink:0;opacity:.5;}
  .topbar-menu-item+.topbar-menu-item{border-top:1px solid var(--line-2);}

  /* ── Merged Filters in Topbar (mobile, on scroll) ── */

  /* ── Download Confirm Popup ── */
  .download-confirm{position:fixed;inset:0;z-index:500;background:rgba(15,14,12,.5);display:none;align-items:center;justify-content:center;padding:24px;backdrop-filter:blur(4px);}
  .download-confirm.open{display:flex;}
  .download-confirm-panel{width:min(400px,100%);padding:36px 32px;background:var(--paper);box-shadow:0 24px 60px rgba(0,0,0,.2);text-align:center;}
  .download-confirm-icon{margin-bottom:16px;color:var(--teal);opacity:.6;}
  .download-confirm-title{font-family:var(--serif);font-size:1.5rem;font-weight:300;margin-bottom:8px;color:var(--ink);}
  .download-confirm-text{font-size:.82rem;color:var(--mist);line-height:1.7;margin-bottom:24px;}
  .download-confirm-text strong{color:var(--ink);font-weight:500;}
  .download-confirm-actions{display:flex;gap:10px;justify-content:center;}
  .download-confirm-actions .btn{min-width:120px;}

  /* ── Hero (full-bleed cinematic cover) ── */
  .hero{position:relative;min-height:94vh;display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center;overflow:hidden;padding:80px 40px 72px;}
  .hero-bg{position:absolute;inset:0;background:url('{{ hero_image or "/assets/stories/abhin-gayathry/01.jpg" }}') center/cover;z-index:0;}
  .hero-video{position:absolute;inset:0;z-index:0;width:100%;height:100%;object-fit:cover;}
  .hero-overlay{position:absolute;inset:0;background:linear-gradient(180deg,rgba(26,24,20,.6) 0%,rgba(26,24,20,.45) 20%,rgba(26,24,20,.4) 40%,rgba(26,24,20,.55) 65%,rgba(26,24,20,.92) 100%);z-index:1;}
  .hero-copy{position:relative;z-index:2;max-width:680px;}
  .hero-monogram{height:48px;width:auto;filter:brightness(0) invert(1) drop-shadow(0 2px 16px rgba(0,0,0,.4));opacity:.8;margin-bottom:48px;}
  .hero-label{font-size:.52rem;letter-spacing:.42em;text-transform:uppercase;color:var(--gold-2);margin-bottom:24px;font-weight:400;text-shadow:0 2px 12px rgba(0,0,0,.3);}
  .hero-label::before,.hero-label::after{content:'';display:inline-block;width:20px;height:1px;background:var(--gold);opacity:.35;vertical-align:middle;margin:0 14px;}
  .hero h1{font-family:var(--serif);font-size:clamp(3.6rem,8vw,7.2rem);font-weight:300;line-height:.94;margin:0 0 28px;color:var(--ivory);text-shadow:0 4px 40px rgba(0,0,0,.35),0 1px 6px rgba(0,0,0,.2);}
  .hero h1 em{font-style:italic;color:var(--gold-2);}
  .hero-sub{font-size:.88rem;line-height:1.9;color:rgba(244,237,224,.7);max-width:460px;margin:0 auto 36px;text-shadow:0 1px 8px rgba(0,0,0,.3);}
  .hero-meta{display:flex;align-items:center;justify-content:center;gap:20px;}
  .meta-item{display:flex;align-items:center;gap:8px;color:rgba(244,237,224,.55);font-size:.58rem;letter-spacing:.18em;text-transform:uppercase;text-shadow:0 1px 6px rgba(0,0,0,.25);}
  .meta-item svg{flex-shrink:0;color:var(--gold-2);opacity:.6;}
  .meta-value{font-family:var(--serif);font-size:1.05rem;color:rgba(244,237,224,.85);font-weight:400;letter-spacing:0;text-transform:none;}
  .meta-dot{width:3px;height:3px;border-radius:50%;background:rgba(244,237,224,.2);flex-shrink:0;}
  .hero-scroll-hint{position:absolute;bottom:32px;left:50%;transform:translateX(-50%);z-index:2;display:flex;flex-direction:column;align-items:center;gap:8px;color:rgba(244,237,224,.3);font-size:.46rem;letter-spacing:.2em;text-transform:uppercase;animation:heroFloat 2.5s ease-in-out infinite;}
  @keyframes heroFloat{0%,100%{transform:translateX(-50%) translateY(0);}50%{transform:translateX(-50%) translateY(-6px);}}
  .hero-scroll-hint svg{opacity:.4;}

  .gallery-wrap{padding:72px 56px 120px;}
  .section-head{display:flex;align-items:end;justify-content:space-between;gap:40px;margin-bottom:40px;}
  .section-title{font-family:var(--serif);font-size:clamp(2.6rem,5vw,4.8rem);font-weight:300;line-height:1.08;color:var(--ink);}
  .section-title em{font-style:italic;color:var(--teal);}
  .section-note{max-width:460px;color:var(--ink-soft);font-size:.86rem;line-height:1.8;}
  .photo-grid{position:relative;width:100%;}
  .photo-card,.quote-card{position:absolute;background:var(--ivory-2);overflow:hidden;border-radius:6px;transition:transform .35s ease,opacity .35s ease,box-shadow .35s ease;box-shadow:0 2px 12px rgba(26,24,20,.06);}
  .photo-card:hover{box-shadow:0 8px 32px rgba(26,24,20,.12);}
  .photo-card.hidden,.quote-card.hidden{display:none!important;}
  .photo-card img{width:100%;height:auto;display:block;transition:transform .5s cubic-bezier(.25,.46,.45,.94),filter .4s;}
  .photo-card:hover img{transform:scale(1.03);filter:saturate(1.05);}
  .quote-card{padding:42px 34px;background:var(--ink);color:var(--ivory);border-radius:6px;}
  .quote-card blockquote{font-family:var(--serif);font-size:clamp(1.45rem,2.4vw,2.25rem);font-style:italic;font-weight:300;line-height:1.32;color:var(--ivory);}
  .quote-card cite{display:block;margin-top:22px;font-style:normal;font-size:.62rem;letter-spacing:.24em;text-transform:uppercase;color:var(--gold-2);}
  .photo-overlay{position:absolute;inset:0;display:flex;flex-direction:column;justify-content:space-between;padding:14px;background:linear-gradient(180deg,rgba(26,24,20,.44),rgba(26,24,20,0) 42%,rgba(26,24,20,.55));opacity:0;transition:opacity .25s;}
  .photo-card:hover .photo-overlay,.photo-card.selected .photo-overlay{opacity:1;}
  .photo-id{align-self:flex-start;color:var(--ivory);font-size:.58rem;letter-spacing:.24em;text-transform:uppercase;text-shadow:0 2px 12px rgba(0,0,0,.35);}
  .photo-controls{display:flex;gap:8px;align-self:flex-end;}
  .icon-btn{width:40px;height:40px;border:1px solid rgba(244,237,224,.4);background:rgba(26,24,20,.3);border-radius:50%;color:var(--ivory);display:grid;place-items:center;transition:all .25s;text-decoration:none;backdrop-filter:blur(4px);}
  .icon-btn:hover{background:rgba(244,237,224,.9);color:var(--teal);border-color:rgba(244,237,224,.9);}
  .photo-card.selected .select-btn{background:var(--gold);border-color:var(--gold);color:var(--teal-deep);}
  .photo-card.selected::after{content:'Selected';position:absolute;top:14px;right:14px;padding:6px 12px;background:var(--gold);color:var(--teal-deep);font-size:.52rem;letter-spacing:.2em;text-transform:uppercase;border-radius:3px;}

  .album-drawer{position:fixed;right:0;top:0;bottom:0;z-index:220;width:min(460px,100%);background:var(--paper);box-shadow:-24px 0 60px rgba(26,24,20,.18);transform:translateX(100%);transition:transform .35s cubic-bezier(.22,1,.36,1);display:flex;flex-direction:column;}
  .album-drawer.open{transform:translateX(0);}
  .drawer-head{padding:28px 28px 22px;border-bottom:1px solid var(--line);display:flex;justify-content:space-between;gap:20px;}
  .drawer-title{font-family:var(--serif);font-size:2rem;font-weight:300;line-height:1;}
  .drawer-close{width:38px;height:38px;border:1px solid var(--line);background:transparent;color:var(--ink);}
  .drawer-body{padding:24px 28px;overflow:auto;flex:1;}
  .selected-list{display:grid;gap:12px;}
  .selected-item{display:grid;grid-template-columns:76px 1fr auto;gap:12px;align-items:center;padding:10px;background:var(--ivory);border:1px solid var(--line-2);}
  .selected-item img{width:76px;height:58px;object-fit:cover;}
  .selected-item strong{font-family:var(--serif);font-size:1.1rem;font-weight:400;display:block;}
  .selected-item span{font-size:.68rem;letter-spacing:.14em;text-transform:uppercase;color:var(--mist);}
  .mini-remove{width:32px;height:32px;border:1px solid var(--line);background:transparent;color:var(--ink);}
  .drawer-foot{padding:22px 28px;border-top:1px solid var(--line);display:grid;gap:12px;}
  .textarea-label{font-size:.6rem;letter-spacing:.22em;text-transform:uppercase;color:var(--mist);}
  .notes,.selector-name{width:100%;padding:13px 14px;border:1px solid var(--line);background:var(--ivory);color:var(--ink);line-height:1.6;}
  .notes{min-height:94px;resize:vertical;}
  .selector-name:focus,.notes:focus{outline:1px solid var(--teal);}
  .export-output{display:none;padding:14px;background:var(--ivory);border:1px solid var(--line);font-size:.78rem;line-height:1.7;color:var(--ink-soft);white-space:pre-wrap;}
  .export-output.visible{display:block;}

  .lightbox{position:fixed;inset:0;z-index:300;background:rgba(15,14,12,.97);display:none;flex-direction:column;color:var(--ivory);}
  .lightbox.open{display:flex;}
  .lb-stage{flex:1;display:flex;align-items:center;justify-content:center;position:relative;min-height:0;overflow:hidden;}
  .lb-stage img{max-width:92vw;max-height:100%;object-fit:contain;box-shadow:0 20px 60px rgba(0,0,0,.4);}
  .lightbox .lb-close,.lightbox .lb-prev,.lightbox .lb-next{position:absolute;border:1px solid rgba(244,237,224,.22);background:rgba(244,237,224,.06);color:var(--ivory);z-index:2;transition:background .2s;}
  .lightbox .lb-close:hover,.lightbox .lb-prev:hover,.lightbox .lb-next:hover{background:rgba(244,237,224,.14);}
  .lb-close{top:16px;right:16px;width:48px;height:48px;font-size:1.5rem;border-radius:50%;background:rgba(244,237,224,.15);border:1px solid rgba(244,237,224,.35);backdrop-filter:blur(8px);}
  .lb-close:hover{background:rgba(244,237,224,.3);border-color:rgba(244,237,224,.5);}
  .lb-prev,.lb-next{top:50%;width:44px;height:44px;border-radius:50%;transform:translateY(-50%);font-size:1.6rem;}
  .lb-prev{left:16px;}.lb-next{right:16px;}
  .lb-bar{flex-shrink:0;display:flex;align-items:center;justify-content:space-between;gap:16px;padding:14px 24px;background:rgba(26,24,20,.7);border-top:1px solid rgba(244,237,224,.08);backdrop-filter:blur(12px);}
  .lb-bar-info{display:flex;flex-direction:column;gap:2px;min-width:0;}
  .lb-bar-id{font-size:.58rem;letter-spacing:.22em;text-transform:uppercase;color:var(--gold-2);}
  .lb-bar-title{font-size:.78rem;color:rgba(244,237,224,.6);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
  .lb-bar-actions{display:flex;gap:8px;flex-shrink:0;}
  .lb-action{display:flex;align-items:center;gap:6px;padding:9px 14px;border:1px solid rgba(244,237,224,.18);background:transparent;color:var(--ivory);font-size:.6rem;letter-spacing:.14em;text-transform:uppercase;text-decoration:none;white-space:nowrap;transition:background .2s,border-color .2s,color .2s;}
  .lb-action:hover{background:rgba(244,237,224,.1);border-color:rgba(244,237,224,.3);}
  .lb-action.active{background:var(--gold);color:var(--teal-deep);border-color:var(--gold);}
  .lb-action-print{border-color:rgba(184,150,90,.5);color:var(--gold-2);}
  .lb-action-print:hover{background:rgba(184,150,90,.14);border-color:var(--gold);}

  .print-banner{display:flex;align-items:center;gap:20px;padding:18px 24px;margin-bottom:36px;background:linear-gradient(135deg,rgba(29,89,89,.06),rgba(184,150,90,.08));border:1px solid rgba(184,150,90,.22);position:relative;overflow:hidden;}
  .print-banner::before{content:'';position:absolute;left:0;top:0;bottom:0;width:3px;background:var(--gold);}
  .print-banner-icon{flex-shrink:0;width:44px;height:44px;display:grid;place-items:center;background:rgba(184,150,90,.1);border:1px solid rgba(184,150,90,.2);color:var(--gold);}
  .print-banner-text{flex:1;min-width:0;}
  .print-banner-text strong{display:block;font-family:var(--serif);font-size:1.1rem;font-weight:400;color:var(--ink);margin-bottom:2px;}
  .print-banner-text span{font-size:.76rem;color:var(--mist);letter-spacing:.02em;}
  .print-banner-cart{display:flex;align-items:center;gap:10px;flex-shrink:0;}
  .print-cart-pill{padding:8px 14px;background:var(--gold);color:var(--teal-deep);font-size:.6rem;letter-spacing:.16em;text-transform:uppercase;border:0;transition:background .2s;}
  .print-cart-pill:hover{background:var(--gold-2);}
  .print-cart-pill.empty{background:transparent;border:1px solid var(--line);color:var(--mist);}

  .print-add-btn{background:rgba(184,150,90,.15)!important;border-color:rgba(184,150,90,.4)!important;color:var(--gold-2)!important;}
  .print-add-btn:hover{background:rgba(184,150,90,.3)!important;}

  .print-overlay{position:fixed;inset:0;z-index:400;background:rgba(15,14,12,.6);display:none;align-items:center;justify-content:center;padding:24px;backdrop-filter:blur(4px);}
  .print-overlay.open{display:flex;}
  .print-panel{width:min(540px,100%);max-height:88vh;background:var(--paper);display:flex;flex-direction:column;box-shadow:0 30px 80px rgba(0,0,0,.3);}
  .print-panel-head{padding:24px 28px 20px;border-bottom:1px solid var(--line);display:flex;justify-content:space-between;align-items:flex-start;gap:16px;}
  .print-panel-title{font-family:var(--serif);font-size:1.8rem;font-weight:300;line-height:1.1;}
  .print-panel-close{width:36px;height:36px;border:1px solid var(--line);background:transparent;color:var(--ink);flex-shrink:0;}
  .print-panel-body{padding:24px 28px;overflow-y:auto;flex:1;}
  .print-cart-list{display:grid;gap:14px;margin-bottom:20px;}
  .print-cart-item{display:grid;grid-template-columns:68px 1fr auto;gap:14px;align-items:start;padding:14px;background:var(--ivory);border:1px solid var(--line-2);}
  .print-cart-item img{width:68px;height:52px;object-fit:cover;display:block;}
  .print-cart-item-info{min-width:0;}
  .print-cart-item-id{font-family:var(--serif);font-size:1rem;font-weight:400;}
  .print-cart-size-row{display:flex;gap:6px;margin-top:8px;}
  .print-size-btn{padding:6px 12px;border:1px solid var(--line);background:var(--ivory);color:var(--ink-soft);font-size:.62rem;letter-spacing:.12em;text-transform:uppercase;transition:all .2s;}
  .print-size-btn.active{background:var(--teal);border-color:var(--teal);color:var(--ivory);}
  .print-size-btn:hover:not(.active){border-color:var(--teal);color:var(--teal);}
  .print-qty-row{display:flex;align-items:center;gap:8px;margin-top:8px;}
  .print-qty-btn{width:28px;height:28px;border:1px solid var(--line);background:var(--ivory);color:var(--ink);font-size:.9rem;display:grid;place-items:center;}
  .print-qty-val{font-size:.82rem;min-width:20px;text-align:center;}
  .print-cart-price{font-family:var(--serif);font-size:1.1rem;color:var(--teal);white-space:nowrap;}
  .print-cart-remove{width:28px;height:28px;border:1px solid var(--line);background:transparent;color:var(--mist);font-size:.7rem;margin-top:6px;}
  .print-add-more{width:100%;padding:14px;border:1px dashed var(--line);background:transparent;color:var(--mist);font-size:.72rem;letter-spacing:.14em;text-transform:uppercase;transition:border-color .2s,color .2s;margin-bottom:24px;}
  .print-add-more:hover{border-color:var(--teal);color:var(--teal);}
  .print-subtotal{padding:14px 0;border-top:1px solid var(--line);border-bottom:1px solid var(--line);display:flex;justify-content:space-between;font-family:var(--serif);font-size:1.2rem;margin-bottom:24px;}
  .print-subtotal span{color:var(--teal);}
  .print-form-fields{display:grid;gap:12px;margin-bottom:20px;}
  .print-input{width:100%;padding:13px 14px;border:1px solid var(--line);background:var(--ivory);color:var(--ink);font-family:var(--sans);font-size:.88rem;line-height:1.5;}
  .print-input:focus{outline:1px solid var(--teal);border-color:var(--teal);}
  .print-textarea{min-height:80px;resize:vertical;}
  .print-panel-foot{padding:18px 28px;border-top:1px solid var(--line);display:grid;gap:10px;}
  .print-note{font-size:.7rem;color:var(--mist);text-align:center;line-height:1.6;}
  .print-wa-btn{display:flex;align-items:center;justify-content:center;gap:8px;}
  .print-success{display:none;text-align:center;padding:48px 24px;}
  .print-success.visible{display:block;}
  .print-success-icon{font-size:2.4rem;margin-bottom:16px;}
  .print-success h3{font-family:var(--serif);font-size:1.6rem;font-weight:300;margin-bottom:8px;}
  .print-success p{color:var(--mist);font-size:.86rem;line-height:1.7;}
  .print-empty{text-align:center;padding:32px 16px;color:var(--mist);font-size:.86rem;line-height:1.7;}

  .gallery-credit{padding:72px 56px;background:var(--ivory-2);border-top:1px solid var(--line-2);}
  .credit-inner{display:flex;align-items:center;gap:24px;max-width:600px;margin:0 auto;}
  .credit-line{flex:1;height:1px;background:var(--line);}
  .credit-link{display:flex;flex-direction:column;align-items:center;gap:8px;text-decoration:none;color:var(--ink-soft);transition:color .3s;}
  .credit-link:hover{color:var(--teal);}
  .credit-logo{height:28px;width:auto;opacity:.55;transition:opacity .3s;}
  .credit-link:hover .credit-logo{opacity:.85;}
  .credit-text{font-size:.72rem;letter-spacing:.16em;text-transform:uppercase;white-space:nowrap;}
  .credit-text strong{font-weight:500;color:var(--ink);}
  .credit-link:hover .credit-text strong{color:var(--teal);}
  .credit-sub{font-size:.62rem;letter-spacing:.2em;text-transform:uppercase;color:var(--mist);}
  .credit-socials{display:flex;gap:16px;margin-top:12px;justify-content:center;}
  .credit-socials a{color:var(--mist);transition:color .3s;}
  .credit-socials a:hover{color:var(--teal);}

  /* ── Authorship Line ── */
  .authorship{padding:20px 40px;text-align:center;background:var(--ivory-2);border-bottom:1px solid var(--line-2);display:flex;align-items:center;justify-content:center;gap:24px;flex-wrap:wrap;}
  .authorship-text{font-size:.54rem;letter-spacing:.3em;text-transform:uppercase;color:var(--mist);font-weight:400;}
  .authorship-text a{color:var(--mist);text-decoration:none;transition:color .3s;}
  .authorship-text a:hover{color:var(--teal);}
  .authorship-dot{width:3px;height:3px;border-radius:50%;background:var(--line);flex-shrink:0;}
  .authorship-btn{display:inline-flex;align-items:center;gap:8px;padding:8px 18px;border:1px solid var(--line);background:var(--ivory);color:var(--ink-soft);font-size:.5rem;letter-spacing:.22em;text-transform:uppercase;text-decoration:none;border-radius:3px;transition:all .3s;white-space:nowrap;}
  .authorship-btn:hover{border-color:var(--teal);color:var(--teal);background:rgba(29,89,89,.03);}

  /* ── Chapter Index ── */
  .chapter-index{padding:72px 56px 80px;background:var(--ivory);text-align:center;}
  .chapter-index-label{font-size:.52rem;letter-spacing:.34em;text-transform:uppercase;color:var(--gold);margin-bottom:10px;font-weight:400;}
  .chapter-index-title{font-family:var(--serif);font-size:clamp(1.8rem,3.5vw,2.8rem);font-weight:300;line-height:1.2;color:var(--ink);margin-bottom:48px;}
  .chapter-index-title em{font-style:italic;color:var(--teal);}
  .chapter-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:1px;background:var(--line-2);max-width:880px;margin:0 auto;border:1px solid var(--line-2);}
  .chapter-card{background:var(--ivory);padding:36px 20px 28px;text-align:center;cursor:pointer;transition:background .35s,transform .35s;}
  .chapter-card:hover{background:var(--ivory-2);}
  .chapter-card-num{font-size:.44rem;letter-spacing:.32em;text-transform:uppercase;color:var(--gold);margin-bottom:12px;font-weight:400;}
  .chapter-card-name{font-family:var(--serif);font-size:1.1rem;font-weight:300;font-style:italic;color:var(--ink);margin-bottom:10px;line-height:1.3;}
  .chapter-card-count{font-size:.46rem;letter-spacing:.24em;text-transform:uppercase;color:var(--mist);font-weight:400;}
  .chapter-card-rule{width:20px;height:1px;background:var(--gold);opacity:.35;margin:14px auto 0;}
  .chapter-card--cta{background:var(--ivory-2);}
  .chapter-card--cta:hover{background:rgba(29,89,89,.04);}
  .chapter-card-icon{margin-bottom:10px;color:var(--gold);opacity:.5;}
  .chapter-card--cta .chapter-card-name{font-style:normal;color:var(--teal);font-weight:400;}
  .chapter-card--cta .chapter-card-count{color:var(--ink-soft);}
  .chapter-index-cta{margin-top:48px;}
  .chapter-index-note{font-size:.78rem;color:var(--ink-soft);line-height:1.8;margin-bottom:18px;font-style:italic;font-family:var(--serif);}
  .chapter-index-btn{display:inline-flex;align-items:center;gap:10px;padding:14px 32px;background:var(--teal);color:var(--ivory);font-size:.58rem;letter-spacing:.24em;text-transform:uppercase;text-decoration:none;border:0;border-radius:3px;transition:background .3s;cursor:pointer;}
  .chapter-index-btn:hover{background:var(--teal-deep);}

  /* ── Section Divider Card ── */
  .section-divider{position:absolute;width:100%;overflow:hidden;border:0;background:var(--ink);border-radius:0;}
  .section-divider-bg{position:absolute;inset:0;background-size:cover;background-position:center;opacity:.35;transition:opacity .8s cubic-bezier(.25,.46,.45,.94);}
  .section-divider:hover .section-divider-bg{opacity:.45;}
  .section-divider-overlay{position:absolute;inset:0;background:linear-gradient(135deg,rgba(15,56,56,.7),rgba(26,24,20,.85));}
  .section-divider--no-cover .section-divider-overlay{background:linear-gradient(135deg,rgba(15,56,56,.95),rgba(26,24,20,.97));}
  .section-divider-content{position:relative;z-index:1;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:10px;height:100%;padding:28px 40px;text-align:center;}
  .section-divider-ornament{display:flex;align-items:center;gap:14px;margin-bottom:4px;}
  .section-divider-line{width:32px;height:1px;background:var(--gold);opacity:.45;flex-shrink:0;}
  .section-divider-dot{width:5px;height:5px;border-radius:50%;border:1px solid var(--gold);opacity:.5;flex-shrink:0;}
  .section-divider-name{font-family:var(--serif);font-size:clamp(1.4rem,3vw,2.2rem);font-weight:300;font-style:italic;color:var(--ivory);letter-spacing:.03em;line-height:1.2;}
  .section-divider-desc{font-family:var(--sans);font-size:.78rem;line-height:1.7;color:rgba(244,237,224,.5);max-width:420px;font-weight:300;font-style:italic;}
  .section-divider-nav{position:absolute;bottom:14px;right:18px;z-index:2;display:flex;gap:6px;}
  .section-divider-nav button{width:30px;height:30px;border:1px solid rgba(244,237,224,.18);background:rgba(244,237,224,.06);border-radius:50%;backdrop-filter:blur(4px);color:rgba(244,237,224,.55);display:grid;place-items:center;cursor:pointer;transition:all .25s;}
  .section-divider-nav button:hover{background:rgba(244,237,224,.14);color:var(--gold-2);border-color:rgba(244,237,224,.35);}

  /* ── Floating Section Jump ── */
  .section-jump{position:fixed;bottom:24px;right:24px;z-index:95;display:none;flex-direction:column;align-items:center;gap:4px;}
  .section-jump.visible{display:flex;}
  .section-jump-btn{width:40px;height:40px;border:1px solid rgba(26,24,20,.1);background:rgba(244,237,224,.95);backdrop-filter:blur(16px);border-radius:50%;color:var(--ink-soft);display:grid;place-items:center;cursor:pointer;transition:all .25s;box-shadow:0 4px 20px rgba(26,24,20,.1);}
  .section-jump-btn:hover{background:var(--teal);color:var(--ivory);border-color:var(--teal);box-shadow:0 6px 24px rgba(29,89,89,.2);}
  .section-jump-label{font-size:.44rem;letter-spacing:.16em;text-transform:uppercase;color:var(--mist);text-align:center;line-height:1;}

  /* ── Explore CTA ── */
  .cg-explore{padding:88px 56px;background:var(--ink);text-align:center;color:var(--ivory);position:relative;overflow:hidden;}
  .cg-explore::before{content:'';position:absolute;top:-60px;left:50%;transform:translateX(-50%);width:1px;height:60px;background:linear-gradient(180deg,transparent,rgba(184,150,90,.3));}
  .cg-explore-ornament{display:flex;align-items:center;justify-content:center;gap:12px;margin-bottom:28px;}
  .cg-explore-ornament span{width:28px;height:1px;background:var(--gold);opacity:.35;}
  .cg-explore-ornament svg{width:16px;height:16px;color:var(--gold);opacity:.5;}
  .cg-explore-title{font-family:var(--serif);font-size:clamp(1.8rem,3.5vw,2.6rem);font-weight:300;line-height:1.2;margin-bottom:14px;color:var(--ivory);}
  .cg-explore-title em{font-style:italic;color:var(--gold-2);}
  .cg-explore-body{font-size:.84rem;line-height:1.9;color:rgba(244,237,224,.5);max-width:440px;margin:0 auto 32px;}
  .cg-explore-btn{display:inline-flex;align-items:center;gap:10px;padding:14px 32px;border:1px solid rgba(244,237,224,.18);background:transparent;color:var(--ivory);font-size:.62rem;letter-spacing:.24em;text-transform:uppercase;text-decoration:none;transition:all .35s;border-radius:3px;}
  .cg-explore-btn:hover{background:rgba(244,237,224,.08);border-color:var(--gold);color:var(--gold-2);}

  /* ── Review Prompt ── */
  .cg-review{padding:52px 56px;background:var(--ivory-2);text-align:center;border-top:1px solid var(--line-2);}
  .cg-review-text{font-family:var(--serif);font-size:1.15rem;font-weight:300;font-style:italic;color:var(--ink-soft);margin-bottom:12px;}
  .cg-review-sub{font-size:.74rem;color:var(--mist);margin-bottom:22px;line-height:1.7;}
  .cg-review-btn{display:inline-flex;align-items:center;gap:8px;padding:11px 24px;border:1px solid var(--line);background:var(--ivory);color:var(--ink-soft);font-size:.6rem;letter-spacing:.2em;text-transform:uppercase;text-decoration:none;transition:all .3s;border-radius:3px;}
  .cg-review-btn:hover{border-color:var(--gold);color:var(--gold);background:rgba(184,150,90,.04);}
  .cg-review-btn svg{flex-shrink:0;color:var(--gold);}

  /* ── Gallery Footer ── */
  .cg-footer{background:rgba(26,24,20,.97);padding:48px 56px 32px;}
  .cg-footer-inner{display:flex;flex-direction:column;align-items:center;gap:24px;}
  .cg-footer-logo{height:44px;width:auto;opacity:.85;filter:brightness(0) invert(1);}
  .cg-footer-links{display:flex;gap:24px;flex-wrap:wrap;justify-content:center;}
  .cg-footer-links a{color:rgba(244,237,224,.5);font-size:.62rem;letter-spacing:.2em;text-transform:uppercase;text-decoration:none;transition:color .3s;}
  .cg-footer-links a:hover{color:var(--gold-2);}
  .cg-footer-bottom{margin-top:24px;padding-top:20px;border-top:1px solid rgba(244,237,224,.08);text-align:center;}
  .cg-footer-copy{font-size:.6rem;letter-spacing:.14em;color:rgba(244,237,224,.3);line-height:1.8;}

  @media(max-width:980px){
    .topbar{height:44px;padding:0 16px;}
    .topbar img{height:22px;}
    .topbar-home,.topbar #downloadAll{display:none!important;}
    .topbar-hamburger{display:grid;}
    .topbar .selection-pill{padding:6px 10px;font-size:.5rem;letter-spacing:.14em;}
    .hero{min-height:88vh;padding:60px 28px 52px;}
    .hero h1{font-size:clamp(2.8rem,11vw,4.4rem);}
    .hero-monogram{height:36px;margin-bottom:36px;}
    .hero-sub{font-size:.82rem;max-width:320px;margin-bottom:28px;}
    .hero-scroll-hint{bottom:20px;}
    .authorship{padding:18px 24px;}
    .chapter-index{padding:52px 24px 60px;}
    .chapter-grid{grid-template-columns:repeat(2,1fr);}
    .chapter-card{padding:28px 16px 22px;}
    .chapter-card-name{font-size:1rem;}
    .gallery-wrap{padding:48px 20px 90px;}
    .section-head{display:block;}
    .section-note{margin-top:18px;}
    .gallery-credit{padding:48px 24px;}
    .credit-inner{gap:16px;}
    .cg-explore{padding:60px 24px;}
    .cg-review{padding:36px 24px;}
    .cg-footer{padding:36px 24px 24px;}
    .lock-panel{padding:36px 24px;}
    .lock-logo{height:40px;margin-bottom:28px;}
    .lock-copy{font-size:.82rem;}
    .print-banner{flex-wrap:wrap;padding:16px 18px;gap:14px;}
    .print-banner-icon{width:36px;height:36px;}
    .print-banner-text strong{font-size:1rem;}
    .print-banner-cart{width:100%;}
    .print-cart-pill{width:100%;text-align:center;}
    .lb-bar{flex-direction:column;align-items:stretch;padding:12px 16px;gap:10px;}
    .lb-bar-actions{justify-content:stretch;}
    .lb-action{flex:1;justify-content:center;padding:10px 8px;font-size:.55rem;}
    .print-panel{max-height:94vh;}
    .print-cart-item{grid-template-columns:56px 1fr auto;}
    .print-cart-item img{width:56px;height:44px;}
  }
  @media(max-width:560px){
    .topbar{height:40px;padding:0 14px;gap:6px;}
    .top-actions{gap:6px;}
    .btn{padding:10px 14px;font-size:.6rem;}
    .topbar-home{width:32px;height:32px;}
    .hero{min-height:85vh;padding:48px 24px 44px;}
    .hero h1{font-size:clamp(2.4rem,12vw,3.6rem);}
    .hero-label{font-size:.48rem;letter-spacing:.36em;}
    .hero-monogram{height:30px;margin-bottom:28px;}
    .hero-meta{flex-direction:column;gap:10px;}
    .meta-dot{display:none;}
    .chapter-grid{grid-template-columns:1fr 1fr;}
    .chapter-card{padding:24px 14px 20px;}
    .chapter-card-name{font-size:.92rem;}
    .selected-item{grid-template-columns:66px 1fr auto;}
    .selected-item img{width:66px;height:54px;}
    .credit-text{font-size:.6rem;white-space:normal;text-align:center;}
    .section-jump{bottom:16px;right:16px;transform:scale(.8);transform-origin:bottom right;}
    .lb-action span{display:none;}
  }

  /* ── Admin Mode ── */
  .admin-trigger{position:fixed;bottom:16px;left:16px;z-index:450;width:40px;height:40px;border-radius:50%;border:1px solid var(--line-2);background:rgba(244,237,224,.85);backdrop-filter:blur(8px);color:var(--mist);display:grid;place-items:center;cursor:pointer;opacity:.35;transition:opacity .3s,background .25s;}
  .admin-trigger:hover{opacity:1;background:var(--ivory);}
  .admin-trigger.active{opacity:1;background:var(--teal);color:var(--ivory);border-color:var(--teal);}
  .admin-login-overlay{position:fixed;inset:0;z-index:500;background:rgba(15,14,12,.6);display:none;align-items:center;justify-content:center;padding:24px;backdrop-filter:blur(4px);}
  .admin-login-overlay.open{display:flex;}
  .admin-login-panel{width:min(400px,100%);padding:40px 36px;background:var(--paper);box-shadow:0 30px 80px rgba(0,0,0,.3);}
  .admin-login-panel h3{font-family:var(--serif);font-size:1.6rem;font-weight:300;margin-bottom:6px;}
  .admin-login-panel p{font-size:.82rem;color:var(--mist);margin-bottom:24px;line-height:1.6;}
  .admin-login-panel form{display:grid;grid-template-columns:1fr auto;gap:10px;}
  .admin-login-panel input{padding:13px 14px;border:1px solid var(--line);background:var(--ivory);color:var(--ink);}
  .admin-login-panel input:focus{outline:1px solid var(--teal);border-color:var(--teal);}
  .admin-login-error{display:none;margin-top:12px;color:#d44;font-size:.8rem;}
  .admin-login-error.visible{display:block;}

  .admin-bar{position:fixed;bottom:0;left:0;right:0;z-index:440;display:none;align-items:center;justify-content:space-between;gap:16px;padding:12px 24px;background:rgba(29,89,89,.95);backdrop-filter:blur(12px);color:var(--ivory);font-size:.72rem;letter-spacing:.12em;text-transform:uppercase;}
  .admin-bar.visible{display:flex;}
  .admin-bar-left{display:flex;align-items:center;gap:16px;}
  .admin-bar-badge{padding:5px 10px;background:rgba(244,237,224,.15);border-radius:2px;font-variant-numeric:tabular-nums;}
  .admin-bar-actions{display:flex;gap:8px;}
  .admin-bar-btn{padding:8px 14px;border:1px solid rgba(244,237,224,.3);background:transparent;color:var(--ivory);font-size:.62rem;letter-spacing:.14em;text-transform:uppercase;cursor:pointer;transition:background .2s;}
  .admin-bar-btn:hover{background:rgba(244,237,224,.12);}
  .admin-bar-btn.active{background:rgba(244,237,224,.2);border-color:rgba(244,237,224,.5);}

  .photo-card .admin-hide-toggle{position:absolute;top:8px;left:8px;z-index:10;width:32px;height:32px;border-radius:50%;border:1px solid rgba(244,237,224,.5);background:rgba(26,24,20,.5);backdrop-filter:blur(4px);color:var(--ivory);display:none;place-items:center;cursor:pointer;transition:background .2s,border-color .2s;font-size:.7rem;}
  .admin-mode .photo-card .admin-hide-toggle{display:grid;}
  .photo-card .admin-hide-toggle:hover{background:rgba(220,60,60,.7);border-color:rgba(220,60,60,.8);}
  .photo-card.admin-hidden{opacity:.4;position:relative;}
  .photo-card.admin-hidden::before{content:'HIDDEN';position:absolute;top:50%;left:50%;transform:translate(-50%,-50%);z-index:5;padding:6px 14px;background:rgba(220,60,60,.85);color:#fff;font-size:.55rem;letter-spacing:.2em;text-transform:uppercase;pointer-events:none;}
  .photo-card.admin-hidden .admin-hide-toggle{background:rgba(60,180,60,.5);border-color:rgba(60,180,60,.6);}
  .photo-card.admin-hidden .admin-hide-toggle:hover{background:rgba(60,180,60,.7);}

  @media(max-width:560px){
    .admin-bar{flex-wrap:wrap;gap:10px;padding:10px 16px;}
    .admin-trigger{bottom:60px;}
  }
</style>
</head>
<body>
<div class="screen-lock" id="screenLock">
  <div class="lock-panel">
    <img src="/assets/logo/logo-white.png" class="lock-logo" alt="Memorygraph Wedding Studio"/>
    <div class="lock-label">Private Client Gallery</div>
    <h1 class="lock-title">Your memories,<br><em>kept quietly.</em></h1>
    <div class="lock-divider"></div>
    <p class="lock-copy">{{ lock_copy or "Enter the gallery password to view, download, and shortlist photographs for your wedding album." }}</p>
    <form class="lock-form" id="lockForm">
      <input type="password" id="passwordInput" placeholder="Enter gallery password" autocomplete="current-password" required/>
      <button class="btn gold" type="submit">Unlock Gallery</button>
    </form>
    <p class="lock-error" id="lockError">That password does not match this gallery.</p>
  </div>
</div>

<!-- Admin trigger (discreet key icon) -->
<button class="admin-trigger" id="adminTrigger" type="button" title="Admin mode" aria-label="Admin mode">
  <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><rect x="3" y="11" width="18" height="11" rx="1"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/><circle cx="12" cy="16" r="1"/></svg>
</button>

<div class="admin-login-overlay" id="adminLoginOverlay">
  <div class="admin-login-panel">
    <h3>Admin access</h3>
    <p>Enter admin password to manage gallery visibility.</p>
    <form id="adminLoginForm">
      <input type="password" id="adminPasswordInput" placeholder="Admin password" autocomplete="off" required/>
      <button class="btn" type="submit">Login</button>
    </form>
    <p class="admin-login-error" id="adminLoginError">Invalid admin password.</p>
  </div>
</div>

<div class="admin-bar" id="adminBar">
  <div class="admin-bar-left">
    <span>Admin Mode</span>
    <span class="admin-bar-badge"><span id="hiddenCount">0</span> hidden</span>
  </div>
  <div class="admin-bar-actions">
    <button class="admin-bar-btn" type="button" id="adminFilterHidden">Show Hidden Only</button>
    <button class="admin-bar-btn" type="button" id="adminLogout">Log Out</button>
  </div>
</div>

<main class="gallery-app" id="galleryApp" aria-hidden="true">

  <!-- ── Utility Bar (fixed, appears on scroll) ── -->
  <header class="topbar" id="topbar">
    <a href="/" aria-label="Memorygraph home"><img src="/assets/logo/logo-dark.png" alt="Memorygraph Wedding Studio"/></a>
    <div class="top-actions">
      <a href="/" class="btn ghost topbar-home" aria-label="Home">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>
      </a>
      <span class="selection-pill"><span id="selectionCount">0</span> selected</span>
      <button class="btn ghost" type="button" id="downloadAll">Download All</button>
      <button class="topbar-hamburger" type="button" id="topbarHamburger" aria-label="Menu">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="18" x2="21" y2="18"/></svg>
      </button>
    </div>
    <div class="topbar-menu" id="topbarMenu">
      <a href="/" class="topbar-menu-item">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>
        Home
      </a>
      <button class="topbar-menu-item" type="button" id="menuDownloadAll">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M12 3v12M7 10l5 5 5-5"/><path d="M5 20h14"/></svg>
        Download All Photos
      </button>
      <button class="topbar-menu-item" type="button" id="menuLockGallery">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><rect x="3" y="11" width="18" height="11" rx="1"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
        Lock Gallery
      </button>
    </div>
  </header>

  <!-- ── Hero (full-bleed cinematic cover) ── -->
  <section class="hero" id="heroSection">
    {% if hero_video %}
    <video class="hero-video" autoplay muted loop playsinline poster="{{ hero_image or '' }}">
      <source src="{{ hero_video }}" type="video/mp4"/>
    </video>
    {% endif %}
    <div class="hero-bg" {% if hero_video %}style="display:none"{% endif %}></div>
    <div class="hero-overlay"></div>
    <div class="hero-copy">
      <img src="/assets/logo/logo-white.png" alt="Memorygraph" class="hero-monogram"/>
      <div class="hero-label">Private Wedding Gallery</div>
      <h1>{{ couple_names or title }}</h1>
      <p class="hero-sub">{{ intro }}</p>
      <div class="hero-meta">
        <div class="meta-item">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="2" y="2" width="20" height="20" rx="2"/><circle cx="8.5" cy="8.5" r="2"/><path d="M21 15l-5-5L5 21"/></svg>
          <span class="meta-value" id="imageTotal">0</span> frames
        </div>
        <span class="meta-dot"></span>
        <div class="meta-item">
          <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><rect x="3" y="11" width="18" height="11" rx="1"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
          Private gallery
        </div>
      </div>
    </div>
    <div class="hero-scroll-hint">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 5v14M5 12l7 7 7-7"/></svg>
    </div>
  </section>

  <!-- ── Authorship Line ── -->
  <div class="authorship">
    <p class="authorship-text">Photographed &amp; filmed by <a href="/">Memorygraph</a> &middot; Kerala</p>
    <span class="authorship-dot"></span>
    <a href="/" class="authorship-btn">
      Visit Memorygraphweddings.com
      <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
    </a>
  </div>

  <!-- ── Chapter Index (section entry cards) ── -->
  {% if gallery_sections and gallery_sections.length %}
  <section class="chapter-index" id="chapterIndex">
    <div class="chapter-index-label">Chapters</div>
    <h2 class="chapter-index-title">Your wedding <em>story,</em> in chapters.</h2>
    <div class="chapter-grid" id="chapterGrid">
      {% for section in gallery_sections %}
      <div class="chapter-card" data-chapter-name="{{ section.name }}">
        <div class="chapter-card-num">{% if loop.index < 10 %}0{% endif %}{{ loop.index }}</div>
        <div class="chapter-card-name">{{ section.name }}</div>
        <div class="chapter-card-count" data-section-count="{{ section.name }}">—</div>
        <div class="chapter-card-rule"></div>
      </div>
      {% endfor %}
      <div class="chapter-card chapter-card--cta" id="chapterKnowMore">
        <div class="chapter-card-icon">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14l-5-4.87 6.91-1.01L12 2z"/></svg>
        </div>
        <div class="chapter-card-name">Know more about us</div>
        <div class="chapter-card-count">Memorygraph Weddings</div>
        <div class="chapter-card-rule"></div>
      </div>
    </div>
    <div class="chapter-index-cta">
      <p class="chapter-index-note">Scroll to explore, or jump to any chapter.</p>
      <button class="chapter-index-btn" type="button" id="scrollToGallery">
        Begin viewing
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 5v14M5 12l7 7 7-7"/></svg>
      </button>
    </div>
  </section>
  {% endif %}

  <section class="gallery-wrap" id="galleryWrap">
    <div class="section-head">
      <div>
        <div class="eyebrow">Choose for album</div>
        <h2 class="section-title">{{ selection_title | safe }}</h2>
      </div>
      <p class="section-note">{{ selection_note }}</p>
    </div>
    <div class="print-banner" id="printBanner">
      <div class="print-banner-icon">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4"><rect x="2" y="2" width="20" height="20" rx="1"/><rect x="5" y="5" width="14" height="14"/><path d="M2 2l3 3M22 2l-3 3M2 22l3-3M22 22l-3-3"/></svg>
      </div>
      <div class="print-banner-text">
        <strong>Print & frame service</strong>
        <span>A4 &#8212; &#8377;500 &middot; A3 &#8212; &#8377;1,000 &middot; Delivered to your door</span>
      </div>
      <div class="print-banner-cart">
        <button class="print-cart-pill empty" type="button" id="openPrintCart">
          <span id="printCartBadge">0</span> items &middot; View order
        </button>
      </div>
    </div>
    <div class="photo-grid" id="photoGrid"></div>
  </section>

  <section class="gallery-credit">
    <div class="credit-inner">
      <div class="credit-line"></div>
      <a href="/" class="credit-link" aria-label="Memorygraph Wedding Studio">
        <img src="/assets/logo/logo-dark.png" alt="Memorygraph" class="credit-logo"/>
        <span class="credit-text">Photographed by <strong>Memorygraph Wedding Studio</strong></span>
        <span class="credit-sub">Thrissur, Kerala</span>
      </a>
      <div class="credit-line"></div>
    </div>
    <div class="credit-socials">
      <a href="https://www.instagram.com/memorygraph.weddings/" target="_blank" rel="noopener" aria-label="Instagram">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="2" y="2" width="20" height="20" rx="5"/><circle cx="12" cy="12" r="5"/><circle cx="17.5" cy="6.5" r="1.5" fill="currentColor" stroke="none"/></svg>
      </a>
      <a href="https://wa.me/919846238073" target="_blank" rel="noopener" aria-label="WhatsApp">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0C5.37 0 0 5.37 0 12c0 2.12.56 4.16 1.6 5.97L.1 23.9l6.13-1.47A11.94 11.94 0 0012 24c6.63 0 12-5.37 12-12S18.63 0 12 0zm5.9 16.9c-.24.68-1.42 1.3-1.96 1.38-.5.08-1.14.12-1.84-.12-.42-.14-.96-.34-1.66-.66-2.92-1.34-4.82-4.28-4.97-4.48-.14-.2-1.18-1.56-1.18-2.98s.74-2.12.98-2.38c.26-.28.56-.34.74-.34h.54c.18 0 .42-.06.64.5.24.56.82 2 .88 2.14.08.14.12.32.02.5-.08.2-.14.3-.28.46-.14.16-.3.36-.42.48-.14.14-.3.3-.12.58.16.28.74 1.22 1.6 1.98 1.08.96 2 1.26 2.28 1.4.28.14.44.12.6-.06.16-.2.7-.82.88-1.1.2-.28.38-.24.64-.14.26.1 1.62.76 1.9.9.28.14.46.2.54.32.06.1.06.6-.18 1.28z"/></svg>
      </a>
      <a href="https://www.youtube.com/@MemorygraphWeddings" target="_blank" rel="noopener" aria-label="YouTube">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M23.5 6.2s-.2-1.6-.9-2.3c-.9-.9-1.8-.9-2.3-1C17 2.6 12 2.6 12 2.6s-5 0-8.3.3c-.5.1-1.5.1-2.3 1-.7.7-.9 2.3-.9 2.3S.2 8.1.2 10v1.8c0 1.9.3 3.8.3 3.8s.2 1.6.9 2.3c.9.9 2 .9 2.5 1 1.8.2 7.6.2 7.6.2s5 0 8.3-.3c.5-.1 1.5-.1 2.3-1 .7-.7.9-2.3.9-2.3s.3-1.9.3-3.8V10c0-1.9-.3-3.8-.3-3.8zM9.5 15.6V8l6.5 3.8-6.5 3.8z"/></svg>
      </a>
    </div>
  </section>

  <section class="cg-explore">
    <div class="cg-explore-ornament">
      <span></span>
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14l-5-4.87 6.91-1.01L12 2z"/></svg>
      <span></span>
    </div>
    <h3 class="cg-explore-title">There's more to <em>explore.</em></h3>
    <p class="cg-explore-body">Visit our website to see more wedding stories, learn about our collections, or start a conversation.</p>
    <a href="/" class="cg-explore-btn">
      Visit memorygraphweddings.com
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
    </a>
  </section>

  <section class="cg-review">
    <p class="cg-review-text">Loved your gallery?</p>
    <p class="cg-review-sub">Your words mean the world to us. A short review helps other couples find their storyteller.</p>
    <a href="https://g.page/r/memorygraph/review" class="cg-review-btn" target="_blank" rel="noopener">
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
      Leave a Review on Google
    </a>
  </section>

  <footer class="cg-footer">
    <div class="cg-footer-inner">
      <a href="/"><img src="/assets/logo/logo-white.png" alt="Memorygraph Wedding Studio" class="cg-footer-logo"/></a>
      <div class="cg-footer-links">
        <a href="/">Home</a>
        <a href="/stories/">Stories</a>
        <a href="/about/">About</a>
        <a href="/investment/">Investment</a>
        <a href="/blog/">Journal</a>
        <a href="/contact/">Contact</a>
      </div>
    </div>
    <div class="cg-footer-bottom">
      <div class="cg-footer-copy">&copy; {% year %} Memorygraph Wedding Studio &middot; Thrissur, Kerala</div>
      <div class="cg-footer-copy">+91 98462 38073 &middot; memorygraphweddings@gmail.com</div>
    </div>
  </footer>

  <div class="section-jump" id="sectionJump">
    <button class="section-jump-btn" type="button" id="sectionPrev" aria-label="Previous section">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M18 15l-6-6-6 6"/></svg>
    </button>
    <span class="section-jump-label">Section</span>
    <button class="section-jump-btn" type="button" id="sectionNext" aria-label="Next section">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 9l6 6 6-6"/></svg>
    </button>
  </div>

  <!-- ── Download Confirmation ── -->
  <div class="download-confirm" id="downloadConfirm">
    <div class="download-confirm-panel">
      <div class="download-confirm-icon">
        <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.2"><path d="M12 3v12M7 10l5 5 5-5"/><path d="M5 20h14"/></svg>
      </div>
      <div class="download-confirm-title">Download all photos?</div>
      <p class="download-confirm-text">This will download <strong id="downloadCount">0</strong> full-size photographs. This may take a while depending on your connection.</p>
      <div class="download-confirm-actions">
        <button class="btn ghost" type="button" id="downloadCancel">Cancel</button>
        <button class="btn" type="button" id="downloadConfirmBtn">Download All</button>
      </div>
    </div>
  </div>
</main>

<aside class="album-drawer" id="albumDrawer" aria-label="Album selections">
  <div class="drawer-head">
    <div><div class="eyebrow">Album Shortlist</div><h2 class="drawer-title">Selected images</h2></div>
    <button class="drawer-close" type="button" id="closeDrawer" aria-label="Close album picks">x</button>
  </div>
  <div class="drawer-body"><div class="selected-list" id="selectedList"></div></div>
  <div class="drawer-foot">
    <label class="textarea-label" for="selectorName">Your name</label>
    <input class="selector-name" id="selectorName" type="text" placeholder="Name of the person selecting album images" autocomplete="name" required/>
    <label class="textarea-label" for="albumNotes">Notes for album design</label>
    <textarea class="notes" id="albumNotes" placeholder="Example: Keep more candid moments, include parents on the opening spread, avoid too many posed portraits."></textarea>
    <button class="btn gold" type="button" id="copySelection">Copy Selection</button>
    <a class="btn" id="whatsappSelection" href="#" target="_blank" rel="noopener">Send on WhatsApp</a>
    <button class="btn ghost" type="button" id="downloadSelected">Download Selected</button>
    <button class="btn ghost" type="button" id="clearSelection">Clear Picks</button>
    <div class="export-output" id="exportOutput"></div>
  </div>
</aside>

<div class="lightbox" id="galleryLightbox" aria-hidden="true">
  <div class="lb-stage">
    <button class="lb-close" type="button" id="lbClose" aria-label="Close">&times;</button>
    <button class="lb-prev" type="button" id="lbPrev" aria-label="Previous">&#8249;</button>
    <button class="lb-next" type="button" id="lbNext" aria-label="Next">&#8250;</button>
    <img src="" alt="" id="lbImage"/>
  </div>
  <div class="lb-bar">
    <div class="lb-bar-info">
      <span class="lb-bar-id" id="lbBarId"></span>
      <span class="lb-bar-title" id="lbBarTitle"></span>
    </div>
    <div class="lb-bar-actions">
      <button class="lb-action" type="button" id="lbSelect">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>
        <span>Select</span>
      </button>
      <a class="lb-action" id="lbDownload" href="#" download>
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 3v12M7 10l5 5 5-5"/><path d="M5 20h14"/></svg>
        <span>Download</span>
      </a>
      <button class="lb-action lb-action-print" type="button" id="lbOrderPrint">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6"><rect x="2" y="2" width="20" height="20" rx="1"/><rect x="5" y="5" width="14" height="14"/><path d="M2 2l3 3M22 2l-3 3M2 22l3-3M22 22l-3-3"/></svg>
        <span>Order Print</span>
      </button>
    </div>
  </div>
</div>

<div class="print-overlay" id="printOverlay">
  <div class="print-panel">
    <div class="print-panel-head">
      <div>
        <div class="eyebrow">Print & Frame</div>
        <div class="print-panel-title">Your print order</div>
      </div>
      <button class="print-panel-close" type="button" id="closePrintPanel">&times;</button>
    </div>
    <div class="print-panel-body" id="printPanelBody">
      <div class="print-cart-list" id="printCartList"></div>
      <button class="print-add-more" type="button" id="printAddMore">+ Add another photo</button>
      <div class="print-subtotal" id="printSubtotal">Order total <span>&#8377;0</span></div>
      <div class="eyebrow" style="margin-bottom:14px;">Delivery details</div>
      <div class="print-form-fields">
        <input class="print-input" type="text" id="printName" placeholder="Full name" required/>
        <input class="print-input" type="tel" id="printPhone" placeholder="Phone number" required/>
        <textarea class="print-input print-textarea" id="printAddress" placeholder="Delivery address (with PIN code)"></textarea>
      </div>
    </div>
    <div class="print-panel-foot">
      <button class="btn gold" type="button" id="submitPrintOrder" style="width:100%;">Place Order</button>
      <a class="btn ghost print-wa-btn" id="printWhatsApp" href="#" target="_blank" rel="noopener">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor"><path d="M17.5 14.4l-2-1c-.3-.1-.5-.1-.7.2l-.9 1.1c-.2.2-.3.2-.6.1-1.7-.8-2.8-1.8-3.7-3.1-.2-.3-.1-.5.1-.7l.6-.7c.2-.2.2-.4.1-.6l-1-2.3c-.2-.6-.5-.5-.7-.5h-.6c-.2 0-.6.1-.9.4C6.5 8 6 9 6 10.1s1 2.7 1.1 2.9c.1.2 2 3.1 4.9 4.3 2 .8 2.8.9 3.8.8.6-.1 1.9-.8 2.2-1.5.3-.8.3-1.4.2-1.5-.1-.2-.3-.3-.7-.5zM12 2C6.5 2 2 6.5 2 12c0 1.8.5 3.5 1.4 5L2 22l5.2-1.4c1.4.8 3 1.2 4.8 1.2 5.5 0 10-4.5 10-10S17.5 2 12 2z"/></svg>
        Send via WhatsApp
      </a>
      <p class="print-note">No payment required now. We will confirm your order and arrange delivery.</p>
    </div>
    <div class="print-success" id="printSuccess">
      <div class="print-success-icon">&#10003;</div>
      <h3>Order placed</h3>
      <p>Your print order has been received. We will contact you shortly to confirm the details and delivery.</p>
      <button class="btn ghost" type="button" id="printSuccessClose" style="margin-top:24px;">Back to gallery</button>
    </div>
  </div>
</div>

<script>
  const galleryPassword = {{ password | json | safe }};
  const gallerySlug = {{ page.fileSlug | json | safe }};
  const galleryTitle = {{ title | json | safe }};
  const coupleNames = {{ (couple_names or title) | json | safe }};
  const whatsappNumber = {{ (whatsapp_number or "919846238073") | json | safe }};
  const gallerySections = {{ gallery_sections | json | safe }} || [];
  const galleryItems = {{ gallery_items | clientGalleryItems(gallery_folder, gallery_drive_images, gallery_sections) | json | safe }};
  const authKey = `memorygraph_gallery_unlocked_${gallerySlug}`;
  const storageKey = `memorygraph_album_selection_${gallerySlug}`;
  const notesKey = `memorygraph_album_notes_${gallerySlug}`;
  const selectorNameKey = `memorygraph_album_selector_${gallerySlug}`;
  let selected = new Set(JSON.parse(localStorage.getItem(storageKey) || '[]'));
  let activeFilter = 'all';
  let sortMode = 'cms';
  let renderedImages = [];
  let lightboxIndex = 0;

  const imageItems = galleryItems
    .map((item, index) => ({...item, order:index, item_type:item.item_type || (item.image ? 'image' : 'quote')}))
    .filter(item => item.item_type === 'image' && item.image)
    .map((item, index) => ({
      id: item.image_code || `MG-${String(index + 1).padStart(3, '0')}`,
      src: item.image,
      cat: (item.category || 'gallery').toLowerCase(),
      title: item.image_name || item.caption || `Image ${index + 1}`,
      caption: item.caption || '',
      order: item.order
    }));

  const screenLock = document.getElementById('screenLock');
  const galleryApp = document.getElementById('galleryApp');
  const photoGrid = document.getElementById('photoGrid');
  const selectionCount = document.getElementById('selectionCount');
  const selectedList = document.getElementById('selectedList');
  const albumDrawer = document.getElementById('albumDrawer');
  const selectorName = document.getElementById('selectorName');
  const albumNotes = document.getElementById('albumNotes');
  const exportOutput = document.getElementById('exportOutput');
  const whatsappSelection = document.getElementById('whatsappSelection');

  document.getElementById('imageTotal').textContent = imageItems.length;
  selectorName.value = localStorage.getItem(selectorNameKey) || '';
  albumNotes.value = localStorage.getItem(notesKey) || '';

  function unlockGallery(){
    screenLock.classList.add('hidden');
    galleryApp.setAttribute('aria-hidden','false');
    sessionStorage.setItem(authKey,'true');
  }

  if(sessionStorage.getItem(authKey) === 'true') unlockGallery();

  document.getElementById('lockForm').addEventListener('submit', (event) => {
    event.preventDefault();
    const input = document.getElementById('passwordInput');
    const error = document.getElementById('lockError');
    if(input.value.trim() === String(galleryPassword || '').trim()){
      error.classList.remove('visible');
      unlockGallery();
    } else {
      error.classList.add('visible');
      input.select();
    }
  });

  const lockAgainBtn = document.getElementById('lockAgain');
  if(lockAgainBtn) lockAgainBtn.addEventListener('click', () => {
    sessionStorage.removeItem(authKey);
    screenLock.classList.remove('hidden');
    galleryApp.setAttribute('aria-hidden','true');
  });

  function saveSelection(){
    localStorage.setItem(storageKey, JSON.stringify([...selected]));
  }

  function sortedGalleryItems(){
    const normalized = galleryItems.map((item, index) => ({...item, order:index, item_type:item.item_type || (item.image ? 'image' : 'quote')}));
    if(sortMode === 'cms') return normalized;
    return [...normalized].sort((a, b) => {
      const aId = a.image_code || '';
      const bId = b.image_code || '';
      if(sortMode === 'selected') return Number(selected.has(bId)) - Number(selected.has(aId)) || a.order - b.order;
      if(sortMode === 'code') return aId.localeCompare(bId) || a.order - b.order;
      if(sortMode === 'name') return (a.image_name || a.caption || a.quote || '').localeCompare(b.image_name || b.caption || b.quote || '') || a.order - b.order;
      return a.order - b.order;
    });
  }

  function buildGallery(){
    const items = sortedGalleryItems();
    renderedImages = [];
    let visibleCount = 0;
    photoGrid.innerHTML = items.map((item) => {
      const type = item.item_type || (item.image ? 'image' : 'quote');
      if(type === 'section'){
        const slug = (item.section_name || '').toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/(^-|-$)/g, '');
        const hasCover = !!item.section_cover;
        const hasDesc = !!(item.section_description && item.section_description.trim());
        return `<div class="section-divider${hasCover ? '' : ' section-divider--no-cover'}" data-type="section" data-section="${escapeAttr(slug)}" id="section-${escapeAttr(slug)}">
          ${hasCover ? `<div class="section-divider-bg" style="background-image:url('${escapeAttr(item.section_cover)}')"></div>` : ''}
          <div class="section-divider-overlay"></div>
          <div class="section-divider-content">
            <div class="section-divider-ornament">
              <span class="section-divider-line"></span>
              <span class="section-divider-dot"></span>
              <span class="section-divider-line"></span>
            </div>
            <span class="section-divider-name">${escapeHtml(item.section_name || '')}</span>
            ${hasDesc ? `<p class="section-divider-desc">${escapeHtml(item.section_description)}</p>` : ''}
          </div>
          <div class="section-divider-nav">
            <button type="button" data-dir="prev" aria-label="Previous section">
              <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M15 18l-6-6 6-6"/></svg>
            </button>
            <button type="button" data-dir="next" aria-label="Next section">
              <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 18l6-6-6-6"/></svg>
            </button>
          </div>
        </div>`;
      }
      if(type === 'quote'){
        return `<article class="quote-card" data-type="quote" data-title="${escapeAttr(item.quote || '')}">
          <blockquote>${escapeHtml(item.quote || '')}</blockquote>
          ${item.quote_by ? `<cite>${escapeHtml(item.quote_by)}</cite>` : ''}
        </article>`;
      }
      const imageIndex = renderedImages.length;
      const id = item.image_code || `MG-${String(imageIndex + 1).padStart(3, '0')}`;
      const image = {
        id,
        src: item.image,
        cat: (item.category || 'gallery').toLowerCase(),
        title: item.image_name || item.caption || `Image ${imageIndex + 1}`,
        caption: item.caption || '',
        order: item.order
      };
      renderedImages.push(image);
      // Admin: skip hidden images for non-admin visitors
      const isHidden = hiddenImageIds.has(image.id);
      if(isHidden && !isAdminMode) return '';
      if(!isHidden) visibleCount++;
      const hiddenClass = isHidden ? ' admin-hidden' : '';
      const hideIcon = isHidden
        ? '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M1 1l22 22"/><path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94"/><path d="M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19"/></svg>'
        : '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>';
      return `<article class="photo-card${selected.has(image.id) ? ' selected' : ''}${hiddenClass}" data-type="image" data-id="${escapeAttr(image.id)}" data-cat="${escapeAttr(image.cat)}" data-title="${escapeAttr((image.title + ' ' + image.caption).toLowerCase())}">
        <button class="admin-hide-toggle" type="button" data-id="${escapeAttr(image.id)}" data-hidden="${isHidden ? '1' : '0'}" title="${isHidden ? 'Unhide' : 'Hide'} this image">${hideIcon}</button>
        <img src="${escapeAttr(image.src)}" alt="${escapeAttr(image.title)}" loading="lazy"/>
        <div class="photo-overlay">
          <span class="photo-id">${escapeHtml(image.id)} · ${escapeHtml(image.title)}</span>
          <div class="photo-controls">
            <button class="icon-btn view-btn" type="button" data-index="${imageIndex}" aria-label="View ${escapeAttr(image.id)}">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M2 12s3.5-6 10-6 10 6 10 6-3.5 6-10 6-10-6-10-6z"/><circle cx="12" cy="12" r="3"/></svg>
            </button>
            <a class="icon-btn download-btn" href="${escapeAttr(image.src)}" download="${escapeAttr(fileNameForImage(image))}" aria-label="Download ${escapeAttr(image.id)}">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7"><path d="M12 3v12M7 10l5 5 5-5"/><path d="M5 20h14"/></svg>
            </a>
            <button class="icon-btn select-btn" type="button" data-id="${escapeAttr(image.id)}" aria-label="Select ${escapeAttr(image.id)}">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M20 6L9 17l-5-5"/></svg>
            </button>
            <button class="icon-btn print-add-btn" type="button" data-index="${imageIndex}" aria-label="Order print of ${escapeAttr(image.id)}">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="2" y="2" width="20" height="20" rx="1"/><rect x="5" y="5" width="14" height="14"/><path d="M2 2l3 3M22 2l-3 3M2 22l3-3M22 22l-3-3"/></svg>
            </button>
          </div>
        </div>
      </article>`;
    }).join('');
    // Update visible image count (exclude hidden for non-admin)
    if(!isAdminMode) document.getElementById('imageTotal').textContent = visibleCount;
    else document.getElementById('imageTotal').textContent = imageItems.length;
    photoGrid.querySelectorAll('.select-btn').forEach(button => button.addEventListener('click', (e) => { e.stopPropagation(); toggleSelect(button.dataset.id); }));
    photoGrid.querySelectorAll('.view-btn').forEach(button => button.addEventListener('click', (e) => { e.stopPropagation(); openLightbox(Number(button.dataset.index)); }));
    photoGrid.querySelectorAll('.download-btn').forEach(link => link.addEventListener('click', (e) => {
      e.stopPropagation();
      e.preventDefault();
      downloadSingleImage(link.href, link.getAttribute('download') || 'image.jpg');
    }));
    photoGrid.querySelectorAll('.print-add-btn').forEach(button => button.addEventListener('click', (e) => { e.stopPropagation(); addToPrintCart(renderedImages[Number(button.dataset.index)]); }));
    photoGrid.querySelectorAll('.admin-hide-toggle').forEach(btn => btn.addEventListener('click', (e) => {
      e.stopPropagation();
      if(!isAdminMode) return;
      const id = btn.dataset.id;
      const wasHidden = btn.dataset.hidden === '1';
      toggleImageVisibility(id, wasHidden ? 'unhide' : 'hide');
    }));
    photoGrid.querySelectorAll('.photo-card').forEach(card => {
      card.addEventListener('click', () => {
        const idx = renderedImages.findIndex(img => img.id === card.dataset.id);
        if (idx >= 0) openLightbox(idx);
      });
      card.style.cursor = 'pointer';
    });
    applyFilters(); // applyFilters calls layoutMasonry
    observeImageLoads();
    setupSectionObserver();
  }

  function toggleSelect(id){
    if(selected.has(id)) selected.delete(id);
    else selected.add(id);
    saveSelection();
    renderSelection();
    document.querySelectorAll(`[data-id="${cssEscape(id)}"]`).forEach(card => card.classList.toggle('selected', selected.has(id)));
    if(sortMode === 'selected') buildGallery();
    else applyFilters();
  }

  function renderSelection(){
    selectionCount.textContent = selected.size;
    const chosen = imageItems.filter(image => selected.has(image.id));
    selectedList.innerHTML = chosen.length ? chosen.map(image => `
      <div class="selected-item">
        <img src="${escapeAttr(image.src)}" alt="${escapeAttr(image.title)}"/>
        <div><strong>${escapeHtml(image.id)}</strong><span>${escapeHtml(image.title)}</span></div>
        <button class="mini-remove" type="button" data-id="${escapeAttr(image.id)}" aria-label="Remove ${escapeAttr(image.id)}">x</button>
      </div>
    `).join('') : '<p style="font-size:.88rem;line-height:1.8;color:var(--ink-soft);">No images selected yet. Choose the frames you want in the album, then come back here to review them.</p>';
    selectedList.querySelectorAll('.mini-remove').forEach(button => button.addEventListener('click', () => toggleSelect(button.dataset.id)));
    updateShareLinks();
  }

  function applyFilters(){
    document.querySelectorAll('.photo-card,.quote-card').forEach((card) => {
      const type = card.dataset.type;
      const matchesFilter = activeFilter === 'all' || (activeFilter === 'selected' && selected.has(card.dataset.id)) || card.dataset.cat === activeFilter || (activeFilter !== 'selected' && type === 'quote');
      card.classList.toggle('hidden', !matchesFilter);
    });
    layoutMasonry();
  }

  // ═══ MASONRY LAYOUT (virtual scroll + batched) ═══
  const MASONRY_GAP = 18;
  const DEFAULT_ASPECT = 3/2; // fallback aspect ratio for images before load
  const RENDER_BUFFER = 800;  // px above/below viewport to pre-render
  let masonryRAF = null;
  let cardAspects = {};  // cache image aspect ratios by src
  let masonryPositions = []; // precomputed {x, y, w, h} for each visible card index

  function getMasonryCols(){
    const w = photoGrid.offsetWidth;
    if(w <= 560) return 1;
    if(w <= 980) return 2;
    return 4;
  }

  function layoutMasonry(){
    if(masonryRAF) cancelAnimationFrame(masonryRAF);
    masonryRAF = requestAnimationFrame(_doMasonryLayout);
  }

  function _doMasonryLayout(){
    const cards = Array.from(photoGrid.children).filter(c => !c.classList.contains('hidden') && c.style.display !== 'none');
    if(!cards.length){ photoGrid.style.height = '0px'; return; }

    const cols = getMasonryCols();
    const gridW = photoGrid.offsetWidth;
    const colW = (gridW - MASONRY_GAP * (cols - 1)) / cols;
    const colHeights = new Array(cols).fill(0);

    // Phase 1: compute positions using cached aspect ratios (no DOM reads per card)
    masonryPositions = [];
    cards.forEach((card, i) => {
      const img = card.querySelector('img');
      const src = img ? img.getAttribute('src') : '';
      const isQuote = card.classList.contains('quote-card');
      const isSection = card.classList.contains('section-divider');

      let h, w, x, y;

      if(isSection){
        // Section dividers: full-width break — wait for all columns to align
        const maxH = Math.max(...colHeights);
        x = 0;
        y = maxH;
        w = gridW;
        // Taller divider if it has a description
        const hasDesc = card.querySelector('.section-divider-desc');
        h = hasDesc ? 210 : 170;
        masonryPositions.push({ x, y, w, h });
        // Reset all columns — small gap after divider
        colHeights.fill(maxH + h + 10);
        return;
      }

      if(isQuote){
        h = 220;
      } else if(cardAspects[src]){
        h = colW / cardAspects[src];
      } else if(img && img.naturalWidth > 0){
        const aspect = img.naturalWidth / img.naturalHeight;
        cardAspects[src] = aspect;
        h = colW / aspect;
      } else {
        h = colW / DEFAULT_ASPECT;
      }

      const col = colHeights.indexOf(Math.min(...colHeights));
      x = col * (colW + MASONRY_GAP);
      y = colHeights[col];

      masonryPositions.push({ x, y, w: colW, h });
      colHeights[col] += h + MASONRY_GAP;
    });

    const totalH = Math.max(...colHeights);
    photoGrid.style.height = totalH + 'px';

    // Phase 2: batch write all positions + virtual scroll (show only near viewport)
    const scrollY = window.scrollY || window.pageYOffset;
    const gridTop = photoGrid.getBoundingClientRect().top + scrollY;
    const vpTop = scrollY - RENDER_BUFFER;
    const vpBottom = scrollY + window.innerHeight + RENDER_BUFFER;

    cards.forEach((card, i) => {
      const p = masonryPositions[i];
      const cardTop = gridTop + p.y;
      const cardBottom = cardTop + p.h;
      const inView = cardBottom >= vpTop && cardTop <= vpBottom;

      card.style.position = 'absolute';
      card.style.width = p.w + 'px';
      card.style.left = p.x + 'px';
      card.style.top = p.y + 'px';
      card.style.visibility = inView ? 'visible' : 'hidden';
      card.style.contentVisibility = inView ? 'visible' : 'hidden';
    });
  }

  // Re-layout on scroll (for virtual scroll culling) + topbar visibility
  let scrollRAF = null;
  const topbarEl = document.getElementById('topbar');
  const heroSectionEl = document.getElementById('heroSection');
  const galleryWrapEl = document.getElementById('galleryWrap');
  window.addEventListener('scroll', () => {
    if(scrollRAF) return;
    scrollRAF = requestAnimationFrame(() => {
      scrollRAF = null;
      _applyVirtualScroll();
      // Show utility bar once hero scrolls out of view
      if(heroSectionEl && topbarEl){
        const heroBottom = heroSectionEl.getBoundingClientRect().bottom;
        const show = heroBottom <= 0;
        topbarEl.classList.toggle('visible', show);
        galleryApp.classList.toggle('bar-visible', show);
      }
    });
  }, {passive:true});

  function _applyVirtualScroll(){
    const cards = Array.from(photoGrid.children).filter(c => !c.classList.contains('hidden') && c.style.display !== 'none');
    if(!masonryPositions.length || cards.length !== masonryPositions.length) return;
    const scrollY = window.scrollY || window.pageYOffset;
    const gridTop = photoGrid.getBoundingClientRect().top + scrollY;
    const vpTop = scrollY - RENDER_BUFFER;
    const vpBottom = scrollY + window.innerHeight + RENDER_BUFFER;
    cards.forEach((card, i) => {
      const p = masonryPositions[i];
      const cardTop = gridTop + p.y;
      const cardBottom = cardTop + p.h;
      const inView = cardBottom >= vpTop && cardTop <= vpBottom;
      card.style.visibility = inView ? 'visible' : 'hidden';
      card.style.contentVisibility = inView ? 'visible' : 'hidden';
    });
  }

  // Re-layout on window resize
  let resizeTimer;
  window.addEventListener('resize', () => {
    clearTimeout(resizeTimer);
    resizeTimer = setTimeout(layoutMasonry, 150);
  });

  // Re-layout when images load (aspect ratio now known)
  function observeImageLoads(){
    photoGrid.querySelectorAll('img').forEach(img => {
      if(img.complete){
        if(img.naturalWidth > 0){
          cardAspects[img.getAttribute('src')] = img.naturalWidth / img.naturalHeight;
        }
        return;
      }
      img.addEventListener('load', function(){
        if(this.naturalWidth > 0){
          cardAspects[this.getAttribute('src')] = this.naturalWidth / this.naturalHeight;
        }
        layoutMasonry();
      }, {once:true});
      img.addEventListener('error', () => layoutMasonry(), {once:true});
    });
  }

  // ═══ SECTION NAVIGATION ═══
  const sectionJump = document.getElementById('sectionJump');
  const sectionSlugs = gallerySections ? gallerySections.map(s => (s.name || '').toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/(^-|-$)/g, '')) : [];
  let currentSectionIdx = -1;

  // Chapter index card clicks → scroll to that section in the grid
  document.querySelectorAll('.chapter-card').forEach(card => {
    card.addEventListener('click', () => {
      const name = card.dataset.chapterName || '';
      const slug = name.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/(^-|-$)/g, '');
      if(slug) scrollToSection(slug);
    });
  });

  // "Know more about us" card → scroll to credits
  const knowMoreCard = document.getElementById('chapterKnowMore');
  if(knowMoreCard){
    knowMoreCard.addEventListener('click', () => {
      const credits = document.querySelector('.gallery-credit');
      if(credits) credits.scrollIntoView({ behavior: 'smooth', block: 'start' });
    });
  }

  // "Begin viewing" button → scroll to gallery wrap
  const scrollToGalleryBtn = document.getElementById('scrollToGallery');
  if(scrollToGalleryBtn){
    scrollToGalleryBtn.addEventListener('click', () => {
      const target = document.getElementById('galleryWrap');
      if(target) target.scrollIntoView({ behavior: 'smooth', block: 'start' });
    });
  }

  // Populate chapter card frame counts
  function updateChapterCounts(){
    if(!gallerySections || !gallerySections.length) return;
    const items = galleryItems;
    gallerySections.forEach((sec, i) => {
      const startFile = sec.start_file || '';
      const startNoExt = startFile.replace(/\.[^.]+$/, '');
      // Find start index of this section
      let startIdx = items.findIndex(item => {
        if(item.item_type === 'section' && item.section_name === sec.name) return true;
        return false;
      });
      if(startIdx < 0) startIdx = 0;
      // Find start of next section
      let endIdx = items.length;
      if(i + 1 < gallerySections.length){
        const nextName = gallerySections[i+1].name;
        const nextIdx = items.findIndex((item, j) => j > startIdx && item.item_type === 'section' && item.section_name === nextName);
        if(nextIdx > 0) endIdx = nextIdx;
      }
      const count = items.slice(startIdx, endIdx).filter(item => (item.item_type || 'image') === 'image' && item.image).length;
      const el = document.querySelector(`[data-section-count="${sec.name}"]`);
      if(el) el.textContent = count + ' frames';
    });
  }
  updateChapterCounts();

  function scrollToSection(slug){
    const el = document.getElementById('section-' + slug);
    if(el) el.scrollIntoView({ behavior: 'smooth', block: 'center' });
  }

  function jumpSection(dir){
    if(!sectionSlugs.length) return;
    let next = currentSectionIdx + dir;
    if(next < 0) next = 0;
    if(next >= sectionSlugs.length) next = sectionSlugs.length - 1;
    scrollToSection(sectionSlugs[next]);
  }

  // Floating prev/next buttons
  document.getElementById('sectionPrev').addEventListener('click', () => jumpSection(-1));
  document.getElementById('sectionNext').addEventListener('click', () => jumpSection(1));

  // Track active section on scroll
  let sectionObserver;
  function setupSectionObserver(){
    if(sectionObserver) sectionObserver.disconnect();
    const dividers = photoGrid.querySelectorAll('.section-divider');
    if(!dividers.length) return;

    // Show floating jump buttons once gallery has sections
    sectionJump.classList.add('visible');

    sectionObserver = new IntersectionObserver((entries) => {
      entries.forEach(e => {
        if(e.isIntersecting){
          const slug = e.target.dataset.section;
          currentSectionIdx = sectionSlugs.indexOf(slug);
        }
      });
    }, { rootMargin: '-30% 0px -60% 0px' });
    dividers.forEach(d => sectionObserver.observe(d));

    // Per-divider prev/next arrows
    dividers.forEach(d => {
      d.querySelectorAll('.section-divider-nav button').forEach(btn => {
        btn.addEventListener('click', (e) => {
          e.stopPropagation();
          const slug = d.dataset.section;
          const idx = sectionSlugs.indexOf(slug);
          const dir = btn.dataset.dir === 'prev' ? -1 : 1;
          const target = idx + dir;
          if(target >= 0 && target < sectionSlugs.length) scrollToSection(sectionSlugs[target]);
        });
      });
    });
  }


  const openDrawerBtn = document.getElementById('openDrawer');
  if(openDrawerBtn) openDrawerBtn.addEventListener('click', () => albumDrawer.classList.add('open'));
  document.getElementById('closeDrawer').addEventListener('click', () => albumDrawer.classList.remove('open'));
  selectorName.addEventListener('input', () => {
    localStorage.setItem(selectorNameKey, selectorName.value);
    updateShareLinks();
  });
  albumNotes.addEventListener('input', () => {
    localStorage.setItem(notesKey, albumNotes.value);
    updateShareLinks();
  });

  function selectionText(){
    const codes = [...selected];
    const selectedBy = selectorName.value.trim();
    const notes = albumNotes.value.trim();
    return `Memorygraph album selection - ${coupleNames}\nGallery: ${galleryTitle}\nSelected by: ${selectedBy || 'Not provided'}\n\nSelected images (${codes.length}):\n${codes.length ? codes.join(', ') : 'None yet'}\n\nNotes:\n${notes || 'No notes added.'}`;
  }

  function requireSelectorName(){
    if(selectorName.value.trim()) return true;
    albumDrawer.classList.add('open');
    selectorName.focus();
    exportOutput.textContent = 'Please enter the name of the person selecting the album images before sending the selection.';
    exportOutput.classList.add('visible');
    return false;
  }

  function updateShareLinks(){
    const text = encodeURIComponent(selectionText());
    whatsappSelection.href = `https://wa.me/${whatsappNumber}?text=${text}`;
  }

  document.getElementById('copySelection').addEventListener('click', async () => {
    if(!requireSelectorName()) return;
    const text = selectionText();
    try {
      await navigator.clipboard.writeText(text);
      exportOutput.textContent = 'Copied to clipboard:\n\n' + text;
    } catch (error) {
      exportOutput.textContent = text;
    }
    exportOutput.classList.add('visible');
  });

  whatsappSelection.addEventListener('click', (event) => {
    if(!requireSelectorName()) event.preventDefault();
  });

  // Download All — show confirmation popup
  function showDownloadConfirm(){
    document.getElementById('downloadCount').textContent = imageItems.length;
    document.getElementById('downloadConfirm').classList.add('open');
    // Close hamburger menu if open
    const menu = document.getElementById('topbarMenu');
    if(menu) menu.classList.remove('open');
  }
  document.getElementById('downloadAll').addEventListener('click', showDownloadConfirm);
  const menuDownloadAll = document.getElementById('menuDownloadAll');
  if(menuDownloadAll) menuDownloadAll.addEventListener('click', showDownloadConfirm);
  document.getElementById('downloadCancel').addEventListener('click', () => {
    document.getElementById('downloadConfirm').classList.remove('open');
  });
  document.getElementById('downloadConfirmBtn').addEventListener('click', () => {
    document.getElementById('downloadConfirm').classList.remove('open');
    downloadImages(imageItems, 'Starting download for the full gallery. Your browser may ask permission for multiple files.');
  });
  document.getElementById('downloadConfirm').addEventListener('click', (e) => {
    if(e.target === e.currentTarget) document.getElementById('downloadConfirm').classList.remove('open');
  });

  document.getElementById('downloadSelected').addEventListener('click', () => {
    const chosen = imageItems.filter(image => selected.has(image.id));
    if(!chosen.length){
      exportOutput.textContent = 'Select at least one image before downloading selected images.';
      exportOutput.classList.add('visible');
      return;
    }
    downloadImages(chosen, `Starting download for ${chosen.length} selected image${chosen.length === 1 ? '' : 's'}.`);
  });

  document.getElementById('clearSelection').addEventListener('click', () => {
    selected = new Set();
    saveSelection();
    buildGallery();
    renderSelection();
    exportOutput.textContent = 'Album picks cleared on this device.';
    exportOutput.classList.add('visible');
  });

  const lightbox = document.getElementById('galleryLightbox');
  const lbImage = document.getElementById('lbImage');
  const lbBarId = document.getElementById('lbBarId');
  const lbBarTitle = document.getElementById('lbBarTitle');
  const lbSelectBtn = document.getElementById('lbSelect');
  const lbDownloadBtn = document.getElementById('lbDownload');
  const lbOrderPrintBtn = document.getElementById('lbOrderPrint');

  function openLightbox(index){
    lightboxIndex = index;
    updateLightbox();
    lightbox.classList.add('open');
    lightbox.setAttribute('aria-hidden','false');
    document.body.style.overflow = 'hidden';
  }

  function updateLightbox(){
    const image = renderedImages[lightboxIndex];
    if(!image) return;
    lbImage.src = image.src;
    lbImage.alt = image.title;
    lbBarId.textContent = image.id;
    lbBarTitle.textContent = image.title;
    lbSelectBtn.classList.toggle('active', selected.has(image.id));
    lbDownloadBtn.href = image.src;
    lbDownloadBtn.download = fileNameForImage(image);
  }

  function closeLightbox(){
    lightbox.classList.remove('open');
    lightbox.setAttribute('aria-hidden','true');
    document.body.style.overflow = '';
  }

  lbSelectBtn.addEventListener('click', () => {
    const image = renderedImages[lightboxIndex];
    if(image) toggleSelect(image.id);
    updateLightbox();
  });
  lbOrderPrintBtn.addEventListener('click', () => {
    const image = renderedImages[lightboxIndex];
    if(image) addToPrintCart(image);
  });

  lbDownloadBtn.addEventListener('click', (e) => {
    e.preventDefault();
    const image = renderedImages[lightboxIndex];
    if(image) downloadSingleImage(image.src, fileNameForImage(image));
  });

  document.getElementById('lbClose').addEventListener('click', closeLightbox);
  document.getElementById('lbPrev').addEventListener('click', () => { lightboxIndex = (lightboxIndex - 1 + renderedImages.length) % renderedImages.length; updateLightbox(); });
  document.getElementById('lbNext').addEventListener('click', () => { lightboxIndex = (lightboxIndex + 1) % renderedImages.length; updateLightbox(); });
  lightbox.querySelector('.lb-stage').addEventListener('click', event => { if(event.target === event.currentTarget || event.target === lbImage) closeLightbox(); });
  document.addEventListener('keydown', event => {
    if(!lightbox.classList.contains('open')) return;
    if(event.key === 'Escape') closeLightbox();
    if(event.key === 'ArrowLeft') document.getElementById('lbPrev').click();
    if(event.key === 'ArrowRight') document.getElementById('lbNext').click();
  });

  function escapeHtml(value){
    return String(value || '').replace(/[&<>"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[char]));
  }

  function escapeAttr(value){
    return escapeHtml(value).replace(/`/g, '&#096;');
  }

  function fileNameForImage(image){
    const extMatch = String(image.src || '').split('?')[0].match(/\.[a-z0-9]+$/i);
    const ext = extMatch ? extMatch[0] : '.jpg';
    const safeTitle = String(image.title || 'image').toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/(^-|-$)/g, '');
    return `${image.id}-${safeTitle || 'image'}${ext}`;
  }

  function downloadSingleImage(src, filename){
    // Cross-origin images (Google Drive etc) need fetch+blob for download to work
    if(src.startsWith('http') && !src.startsWith(location.origin)){
      return fetch(src, {mode:'cors'})
        .then(r => r.blob())
        .then(blob => {
          const url = URL.createObjectURL(blob);
          const link = document.createElement('a');
          link.href = url;
          link.download = filename;
          document.body.appendChild(link);
          link.click();
          link.remove();
          URL.revokeObjectURL(url);
        })
        .catch(() => {
          // Fallback: open in new tab
          window.open(src, '_blank');
        });
    } else {
      const link = document.createElement('a');
      link.href = src;
      link.download = filename;
      link.rel = 'noopener';
      document.body.appendChild(link);
      link.click();
      link.remove();
      return Promise.resolve();
    }
  }

  function downloadImages(images, message){
    if(!images.length) return;
    exportOutput.textContent = message;
    exportOutput.classList.add('visible');
    images.forEach((image, index) => {
      setTimeout(() => {
        downloadSingleImage(image.src, fileNameForImage(image));
      }, index * 300);
    });
  }

  function cssEscape(value){
    if(window.CSS && CSS.escape) return CSS.escape(value);
    return String(value).replace(/"/g, '\\"');
  }

  // ═══ PRINT CART ═══
  const printCartKey = `memorygraph_print_cart_${gallerySlug}`;
  let printCart = JSON.parse(localStorage.getItem(printCartKey) || '[]');
  const printOverlay = document.getElementById('printOverlay');
  const printCartList = document.getElementById('printCartList');
  const printSubtotal = document.getElementById('printSubtotal');
  const printCartBadge = document.getElementById('printCartBadge');
  const openPrintCartBtn = document.getElementById('openPrintCart');
  const printSuccess = document.getElementById('printSuccess');
  const printPanelBody = document.getElementById('printPanelBody');

  function savePrintCart(){
    localStorage.setItem(printCartKey, JSON.stringify(printCart));
    updatePrintBadge();
  }

  function updatePrintBadge(){
    const count = printCart.reduce((s, i) => s + (i.qty || 1), 0);
    printCartBadge.textContent = count;
    openPrintCartBtn.classList.toggle('empty', count === 0);
  }

  function addToPrintCart(image){
    const existing = printCart.find(i => i.imageId === image.id);
    if(existing){
      existing.qty = (existing.qty || 1) + 1;
    } else {
      printCart.push({ imageId: image.id, src: image.src, title: image.title, size: 'A4', qty: 1 });
    }
    savePrintCart();
    openPrintPanel();
  }

  function getPrintTotal(){
    return printCart.reduce((sum, item) => sum + (item.size === 'A3' ? 1000 : 500) * (item.qty || 1), 0);
  }

  function renderPrintCart(){
    if(!printCart.length){
      printCartList.innerHTML = '<div class="print-empty">No items yet. Click the frame icon on any photo, or use the <strong>Order Print</strong> button in the fullscreen view.</div>';
    } else {
      printCartList.innerHTML = printCart.map((item, idx) => `
        <div class="print-cart-item">
          <img src="${escapeAttr(item.src)}" alt="${escapeAttr(item.title)}"/>
          <div class="print-cart-item-info">
            <div class="print-cart-item-id">${escapeHtml(item.imageId)}</div>
            <div class="print-cart-size-row">
              <button class="print-size-btn${item.size === 'A4' ? ' active' : ''}" type="button" data-idx="${idx}" data-size="A4">A4 &middot; &#8377;500</button>
              <button class="print-size-btn${item.size === 'A3' ? ' active' : ''}" type="button" data-idx="${idx}" data-size="A3">A3 &middot; &#8377;1,000</button>
            </div>
            <div class="print-qty-row">
              <button class="print-qty-btn" type="button" data-idx="${idx}" data-dir="-1">&minus;</button>
              <span class="print-qty-val">${item.qty || 1}</span>
              <button class="print-qty-btn" type="button" data-idx="${idx}" data-dir="1">+</button>
              <button class="print-cart-remove" type="button" data-idx="${idx}" title="Remove">&times;</button>
            </div>
          </div>
          <div class="print-cart-price">&#8377;${((item.size === 'A3' ? 1000 : 500) * (item.qty || 1)).toLocaleString('en-IN')}</div>
        </div>
      `).join('');
      printCartList.querySelectorAll('.print-size-btn').forEach(btn => btn.addEventListener('click', () => {
        printCart[Number(btn.dataset.idx)].size = btn.dataset.size;
        savePrintCart();
        renderPrintCart();
      }));
      printCartList.querySelectorAll('.print-qty-btn').forEach(btn => btn.addEventListener('click', () => {
        const idx = Number(btn.dataset.idx);
        const dir = Number(btn.dataset.dir);
        printCart[idx].qty = Math.max(1, (printCart[idx].qty || 1) + dir);
        savePrintCart();
        renderPrintCart();
      }));
      printCartList.querySelectorAll('.print-cart-remove').forEach(btn => btn.addEventListener('click', () => {
        printCart.splice(Number(btn.dataset.idx), 1);
        savePrintCart();
        renderPrintCart();
      }));
    }
    printSubtotal.innerHTML = `Order total <span>&#8377;${getPrintTotal().toLocaleString('en-IN')}</span>`;
    updatePrintWhatsApp();
  }

  function openPrintPanel(){
    printSuccess.classList.remove('visible');
    printPanelBody.style.display = '';
    document.querySelector('.print-panel-foot').style.display = '';
    renderPrintCart();
    printOverlay.classList.add('open');
  }

  function closePrintPanel(){
    printOverlay.classList.remove('open');
  }

  function printOrderText(){
    const name = document.getElementById('printName').value.trim();
    const phone = document.getElementById('printPhone').value.trim();
    const address = document.getElementById('printAddress').value.trim();
    let text = `Memorygraph Print Order\nGallery: ${galleryTitle}\n\n`;
    printCart.forEach(item => {
      text += `${item.imageId} — ${item.size} × ${item.qty || 1} — ₹${((item.size === 'A3' ? 1000 : 500) * (item.qty || 1))}\n`;
    });
    text += `\nTotal: ₹${getPrintTotal().toLocaleString('en-IN')}\n\nName: ${name || 'Not provided'}\nPhone: ${phone || 'Not provided'}\nAddress: ${address || 'Not provided'}`;
    return text;
  }

  function updatePrintWhatsApp(){
    const text = encodeURIComponent(printOrderText());
    document.getElementById('printWhatsApp').href = `https://wa.me/${whatsappNumber}?text=${text}`;
  }

  openPrintCartBtn.addEventListener('click', openPrintPanel);
  document.getElementById('closePrintPanel').addEventListener('click', closePrintPanel);
  printOverlay.addEventListener('click', e => { if(e.target === printOverlay) closePrintPanel(); });
  document.getElementById('printAddMore').addEventListener('click', () => { closePrintPanel(); });

  document.getElementById('submitPrintOrder').addEventListener('click', async () => {
    const name = document.getElementById('printName').value.trim();
    const phone = document.getElementById('printPhone').value.trim();
    const address = document.getElementById('printAddress').value.trim();
    if(!printCart.length){ alert('Add at least one photo to your order.'); return; }
    if(!name || !phone || !address){ alert('Please fill in your name, phone number, and delivery address.'); return; }
    const submitBtn = document.getElementById('submitPrintOrder');
    submitBtn.textContent = 'Placing order...';
    submitBtn.disabled = true;
    try {
      const res = await fetch('/api/order', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({ gallery: galleryTitle, items: printCart, name, phone, address })
      });
      if(res.ok){
        printCart = [];
        savePrintCart();
        printPanelBody.style.display = 'none';
        document.querySelector('.print-panel-foot').style.display = 'none';
        printSuccess.classList.add('visible');
      } else {
        const err = await res.json().catch(() => ({}));
        alert(err.error || 'Order could not be placed. Please try sending via WhatsApp instead.');
      }
    } catch(e) {
      alert('Network error. Please try sending your order via WhatsApp instead.');
    }
    submitBtn.textContent = 'Place Order';
    submitBtn.disabled = false;
  });

  document.getElementById('printSuccessClose').addEventListener('click', closePrintPanel);

  document.querySelectorAll('#printName,#printPhone,#printAddress').forEach(el => el.addEventListener('input', updatePrintWhatsApp));

  updatePrintBadge();

  // ═══ ADMIN MODE ═══
  let isAdminMode = false;
  let adminToken = null;
  let hiddenImageIds = new Set();
  let adminShowHiddenOnly = false;
  const adminTokenKey = `memorygraph_admin_token`;

  const adminTrigger = document.getElementById('adminTrigger');
  const adminLoginOverlay = document.getElementById('adminLoginOverlay');
  const adminBar = document.getElementById('adminBar');
  const hiddenCountEl = document.getElementById('hiddenCount');

  // Fetch hidden images on page load (public endpoint, no auth needed)
  async function fetchHiddenImages(){
    try {
      const res = await fetch(`/api/admin/hidden?slug=${encodeURIComponent(gallerySlug)}`);
      if(res.ok){
        const data = await res.json();
        hiddenImageIds = new Set(data.hidden || []);
      }
    } catch(e){
      // API not available (local dev, etc) — proceed with empty set
      hiddenImageIds = new Set();
    }
  }

  async function toggleImageVisibility(imageId, action){
    if(!adminToken) return;
    try {
      const res = await fetch('/api/admin/hidden', {
        method: 'POST',
        headers: {'Content-Type': 'application/json', 'Authorization': `Bearer ${adminToken}`},
        body: JSON.stringify({ slug: gallerySlug, imageId, action })
      });
      if(res.ok){
        const data = await res.json();
        hiddenImageIds = new Set(data.hidden || []);
        updateHiddenCount();
        buildGallery();
        renderSelection();
      } else if(res.status === 401){
        exitAdminMode();
      }
    } catch(e){
      console.error('Failed to toggle visibility:', e);
    }
  }

  function updateHiddenCount(){
    hiddenCountEl.textContent = hiddenImageIds.size;
  }

  function enterAdminMode(){
    isAdminMode = true;
    document.body.classList.add('admin-mode');
    adminTrigger.classList.add('active');
    adminBar.classList.add('visible');
    adminLoginOverlay.classList.remove('open');
    updateHiddenCount();
    buildGallery();
    renderSelection();
  }

  function exitAdminMode(){
    isAdminMode = false;
    adminToken = null;
    adminShowHiddenOnly = false;
    sessionStorage.removeItem(adminTokenKey);
    document.body.classList.remove('admin-mode');
    adminTrigger.classList.remove('active');
    adminBar.classList.remove('visible');
    document.getElementById('adminFilterHidden').classList.remove('active');
    buildGallery();
    renderSelection();
  }

  // Admin trigger click
  adminTrigger.addEventListener('click', () => {
    if(isAdminMode){
      exitAdminMode();
    } else {
      adminLoginOverlay.classList.add('open');
      document.getElementById('adminPasswordInput').focus();
    }
  });

  // Close admin login on overlay click
  adminLoginOverlay.addEventListener('click', (e) => {
    if(e.target === adminLoginOverlay) adminLoginOverlay.classList.remove('open');
  });

  // Admin login form
  document.getElementById('adminLoginForm').addEventListener('submit', async (e) => {
    e.preventDefault();
    const pw = document.getElementById('adminPasswordInput').value;
    const errorEl = document.getElementById('adminLoginError');
    errorEl.classList.remove('visible');
    try {
      const res = await fetch('/api/admin/auth', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({ password: pw })
      });
      const data = await res.json();
      if(data.ok && data.token){
        adminToken = data.token;
        sessionStorage.setItem(adminTokenKey, adminToken);
        document.getElementById('adminPasswordInput').value = '';
        enterAdminMode();
      } else {
        errorEl.classList.add('visible');
      }
    } catch(err){
      errorEl.textContent = 'Could not reach server. Try again.';
      errorEl.classList.add('visible');
    }
  });

  // Admin logout
  document.getElementById('adminLogout').addEventListener('click', exitAdminMode);

  // Show hidden only filter
  document.getElementById('adminFilterHidden').addEventListener('click', () => {
    adminShowHiddenOnly = !adminShowHiddenOnly;
    document.getElementById('adminFilterHidden').classList.toggle('active', adminShowHiddenOnly);
    applyFilters();
  });

  // Patch applyFilters to respect admin hidden-only filter
  const _origApplyFilters = applyFilters;
  applyFilters = function(){
    _origApplyFilters();
    if(isAdminMode && adminShowHiddenOnly){
      document.querySelectorAll('.photo-card').forEach(card => {
        if(!card.classList.contains('admin-hidden')){
          card.classList.add('hidden');
        }
      });
    }
  };

  // Keyboard shortcut: Ctrl+Shift+A
  document.addEventListener('keydown', (e) => {
    if(e.ctrlKey && e.shiftKey && e.key === 'A'){
      e.preventDefault();
      if(isAdminMode) exitAdminMode();
      else {
        adminLoginOverlay.classList.add('open');
        document.getElementById('adminPasswordInput').focus();
      }
    }
  });

  // Restore admin session from sessionStorage
  async function tryRestoreAdmin(){
    const stored = sessionStorage.getItem(adminTokenKey);
    if(!stored) return false;
    try {
      // Validate token by making a lightweight request
      const res = await fetch('/api/admin/hidden', {
        method: 'POST',
        headers: {'Content-Type': 'application/json', 'Authorization': `Bearer ${stored}`},
        body: JSON.stringify({ slug: gallerySlug, imageId: '__test__', action: 'unhide' })
      });
      if(res.ok || res.status === 400){
        // Token is valid (400 means bad input but auth passed)
        adminToken = stored;
        return true;
      }
    } catch(e){}
    sessionStorage.removeItem(adminTokenKey);
    return false;
  }

  // ═══ HAMBURGER MENU ═══
  const hamburgerBtn = document.getElementById('topbarHamburger');
  const topbarMenu = document.getElementById('topbarMenu');
  if(hamburgerBtn && topbarMenu){
    hamburgerBtn.addEventListener('click', (e) => {
      e.stopPropagation();
      topbarMenu.classList.toggle('open');
    });
    document.addEventListener('click', (e) => {
      if(!topbarMenu.contains(e.target) && e.target !== hamburgerBtn){
        topbarMenu.classList.remove('open');
      }
    });
  }

  // Lock gallery from menu
  const menuLockBtn = document.getElementById('menuLockGallery');
  if(menuLockBtn){
    menuLockBtn.addEventListener('click', () => {
      topbarMenu.classList.remove('open');
      sessionStorage.removeItem(authKey);
      screenLock.classList.remove('hidden');
      galleryApp.setAttribute('aria-hidden','true');
    });
  }

  // Initialize: fetch hidden list, then build
  (async () => {
    await fetchHiddenImages();
    const restored = await tryRestoreAdmin();
    if(restored) enterAdminMode();
    else {
      buildGallery();
      renderSelection();
    }
  })();
</script>
</body>
</html>
